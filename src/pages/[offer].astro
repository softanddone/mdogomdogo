
---
export const prerender = true;

import { products } from "../data/offerproduct";
import Related from "../components/Related.astro"

export async function getStaticPaths() {
  return products.map((product) => ({
    params: { offer: product.slug },
  }));
}

const { offer } = Astro.params;
const currentProduct = products.find((p) => p.slug === offer);

if (!currentProduct) {
  return Astro.redirect("/404");
}

const productList = [currentProduct];
---


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <title>{currentProduct.name + " | Lipa Mdogo Mdogo Kenya"}</title>

  <meta name="description" content={`Buy ${currentProduct.name} on Lipa Mdogo Mdogo in Nairobi & Kiambu. Pay slowly, own it easily. Home delivery available.`} />

  <link rel="canonical" href={`https://mdogomdogodeals.co.ke/${currentProduct.slug}`} />

  <!-- Open Graph -->
  <meta property="og:type" content="product" />
  <meta property="og:title" content={`${currentProduct.name} | Lipa Mdogo Mdogo Kenya`} />
  <meta property="og:description" content={`Buy ${currentProduct.name} on Lipa Mdogo Mdogo. Pay slowly, own it easily.`} />
  <meta property="og:image" content={currentProduct.images[0]} />
  <meta property="og:url" content={`https://mdogomdogodeals.co.ke/${currentProduct.slug}`} />

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={`${currentProduct.name} | Lipa Mdogo Mdogo Kenya`} />
  <meta name="twitter:description" content={`Buy ${currentProduct.name} on Lipa Mdogo Mdogo. Pay slowly, own it easily.`} />
  <meta name="twitter:image" content={currentProduct.images[0]} />
</head>
<body>
  
  {productList.map((product) => (
  <div class="main-product-page-container">

    <div class="main-product-grid">

      {/* Left Side - Image Carousel */}
      <div class="main-image-section">

<div class="main-top">
<button 
  class="main-call"
  onclick="window.location.href='tel:+254720202167'"
>
  Call +254720202167
</button>
          <div></div>

<button class="main-share-button" id="shareBtn">
            <span class="main-share-icon">↗</span>
            Share
          </button>


</div>


        <div class="main-image-carousel">
          {product.images.map((image, idx) => (
            <div class={`main-carousel-image ${idx === 0 ? 'active' : ''}`} data-index={idx}>
              <img 
                src={image} 
                alt={`${product.name} - Image ${idx + 1}`}
                class="main-product-image"
                loading={idx === 0 ? 'eager' : 'lazy'}
              />
            </div>
          ))}

          {/* Navigation Buttons */}
          {product.images.length > 1 && (
            <>
              <button class="main-carousel-btn prev" aria-label="Previous image">
                <span class="main-arrow-icon">‹</span>
              </button>
              <button class="main-carousel-btn next" aria-label="Next image">
                <span class="main-arrow-icon">›</span>
              </button>
            </>
          )}

          {/* Image Indicators */}
          {product.images.length > 1 && (
            <div class="main-carousel-indicators">
              {product.images.map((_, idx) => (
                <button 
                  class={`main-indicator ${idx === 0 ? 'active' : ''}`} 
                  data-index={idx}
                  aria-label={`Go to image ${idx + 1}`}
                />
              ))}
            </div>
          )}
        </div>

        {/* Trust Badges - Below Images on Mobile */}
        <div class="main-trust-badges-mobile">
          <div class="main-trust-badge">
            <div class="main-badge-icon">✓</div>
            <span>Verified Products</span>
          </div>
          <div class="main-trust-badge">
            <div class="main-badge-icon">💳</div>
            <span>Pay After Delivery</span>
          </div>
          <div class="main-trust-badge">
            <div class="main-badge-icon">👥</div>
            <span>500+ Happy Customers</span>
          </div>
        </div>
      </div>

      {/* Right Side - Product Details */}
      <div class="main-details-section">
        {/* Product Header */}
        <div class="main-product-header">
          <h1 class="main-product-title">{product.name}</h1>
          <div class="main-payment-badges">
            <span class="main-badge main-badge-primary">
              <span class="main-badge-icon-inline">✓</span>
              Payment After Delivery
            </span>
            <span class="main-badge main-badge-secondary">
              <span class="main-badge-icon-inline">💰</span>
              Lipa Mdogo Mdogo
            </span>
          </div>
        </div>

        {/* Modern Pricing Card */}
        <div class="main-pricing-card-modern">
          <div class="main-pricing-glow"></div>
          <div class="main-pricing-content">
            <div class="main-pricing-header">
              <h2>Flexible Payment Plan</h2>
              <p class="main-pricing-subtitle">Pay in installments with zero hidden fees</p>
            </div>
            
            <div class="main-pricing-options">
              <div class="main-pricing-option deposit">
                <div class="main-option-label">
                  <span class="main-label-text">Initial Deposit</span>
                  <span class="main-label-badge">One-time</span>
                </div>
                <div class="main-option-amount">
                  <span class="main-currency">KES</span>
                  <span class="main-amount">{product.deposit.toLocaleString()}</span>
                </div>
              </div>
              
              <div class="main-pricing-separator">
                <div class="main-separator-line"></div>
                <span class="main-separator-text">then</span>
                <div class="main-separator-line"></div>
              </div>
              
              <div class="main-pricing-option daily">
                <div class="main-option-label">
                  <span class="main-label-text">Daily Payment</span>
                  <span class="main-label-badge">Flexible</span>
                </div>
                <div class="main-option-amount">
                  <span class="main-currency">KES</span>
                  <span class="main-amount">{product.dailyPayment}</span>
                  <span class="main-period">/day</span>
                </div>
              </div>
            </div>

            <div class="main-pricing-benefits">
              <div class="main-benefit-item">
                <span class="main-check-icon">✓</span>
                <span>No hidden charges</span>
              </div>
              <div class="main-benefit-item">
                <span class="main-check-icon">✓</span>
                <span>Pause anytime</span>
              </div>
              <div class="main-benefit-item">
                <span class="main-check-icon">✓</span>
                <span>100% transparent pricing</span>
              </div>
            </div>
          </div>
        </div>

        {/* Main CTA Button - Desktop */}
        <div class="main-cta-section desktop-only">
          <a 
            href={`https://wa.me/254720202167?text=Hi, I'm interested in ${encodeURIComponent(product.name)}`}
            class="main-cta-button-main"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span class="main-whatsapp-icon">📱</span>
            <span>Order via WhatsApp</span>
            <span class="main-arrow-icon">→</span>
          </a>
          
          <button class="main-share-button" id="shareBtn2">
            <span class="main-share-icon">↗</span>
            Share
          </button>
        </div>

        {/* How It Works Section */}
        <div class="main-how-it-works">
          <h3 class="main-section-title">
            <span class="main-title-decoration"></span>
            How It Works
          </h3>
          <div class="main-steps-container">
            <div class="main-step-card">
              <div class="main-step-header">
                <div class="main-step-number">01</div>
                <h4>Order Now</h4>
              </div>
              <p>Click the WhatsApp button to place your order instantly</p>
            </div>
            <div class="main-step-card">
              <div class="main-step-header">
                <div class="main-step-number">02</div>
                <h4>We Deliver</h4>
              </div>
              <p>Receive your product within 5 hours</p>
            </div>
            <div class="main-step-card">
              <div class="main-step-header">
                <div class="main-step-number">03</div>
                <h4>Pay Deposit After Delivery</h4>
              </div>
              <p>Inspect first, then pay your deposit</p>
            </div>
            <div class="main-step-card">
              <div class="main-step-header">
                <div class="main-step-number">04</div>
                <h4>Daily Payments</h4>
              </div>
              <p>Start daily payment after the free period</p>
            </div>
          </div>
        </div>

        {/* Product Description */}
        <div class="main-product-description">
          <h3 class="main-section-title">
            <span class="main-title-decoration"></span>
            About This Product
          </h3>
          <p class="main-description-text">{product.description}</p>
        </div>

        {/* Key Features */}
        <div class="main-product-features">
          <h3 class="main-section-title">
            <span class="main-title-decoration"></span>
            Key Features
          </h3>
          <div class="main-features-grid">
            {product.features.map((feature) => (
              <div class="main-feature-item">
                <div class="main-feature-icon">✓</div>
                <span>{feature}</span>
              </div>
            ))}
          </div>
        </div>

        {/* Delivery Information */}
        <div class="main-delivery-card">
          <div class="main-delivery-icon">🚚</div>
          <div class="main-delivery-content">
            <h4>Fast & Free Delivery</h4>
            <p><strong>Coverage:</strong> Nairobi, Kiambu & surrounding areas</p>
            <p><strong>Timeline:</strong> 24-48 hours from order confirmation</p>
            <div class="main-delivery-highlight">
              <span class="main-check-icon">✓</span>
              <span>Free delivery • Inspect before payment</span>
            </div>
          </div>
        </div>

        {/* Trust Indicators - Desktop */}
        <div class="main-trust-section desktop-only">
          <div class="main-trust-card">
            <div class="main-trust-icon-wrapper">🛡️</div>
            <div class="main-trust-text">
              <h4>100% Authentic</h4>
              <p>Genuine products guaranteed</p>
            </div>
          </div>

          <div class="main-trust-card">
            <div class="main-trust-icon-wrapper">💳</div>
            <div class="main-trust-text">
              <h4>Flexible Payments</h4>
              <p>Pay at your own pace</p>
            </div>
          </div>

          <div class="main-trust-card">
            <div class="main-trust-icon-wrapper">⭐</div>
            <div class="main-trust-text">
              <h4>500+ Happy Customers</h4>
              <p>Join our satisfied community</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    {/* Mobile Sticky WhatsApp Bar */}
    <div class="main-mobile-sticky-cta">
      <div class="main-sticky-cta-content">
        <div class="main-sticky-price-info">
          <span class="main-sticky-deposit">KES {product.deposit.toLocaleString()}</span>
          <span class="main-sticky-daily">+ KES {product.dailyPayment}/day</span>
        </div>
        <a 
          href={`https://wa.me/254720202167?text=Hi, I'm interested in ${encodeURIComponent(product.name)}`}
          class="main-sticky-whatsapp-btn"
          target="_blank"
          rel="noopener noreferrer"
        >
          <span class="main-whatsapp-icon">📱</span>
          Order Now
        </a>
      </div>
    </div>
  </div>
))}

{/* Related Products Component - Inserted Here */}
<Related />

{/* Share Modal */}
<div class="main-share-modal" id="shareModal">
  <div class="main-share-modal-overlay"></div>
  <div class="main-share-modal-content">
    <div class="main-share-modal-header">
      <h3>Share this product</h3>
      <button class="main-close-modal" id="closeModal">
        <span class="main-close-icon">×</span>
      </button>
    </div>
    <div class="main-share-options">
      <a href="#" class="main-share-option whatsapp-share" id="whatsappShare">
        <div class="main-share-icon whatsapp">📱</div>
        <span>Share via WhatsApp</span>
      </a>
      <a href="#" class="main-share-option facebook-share" id="facebookShare">
        <div class="main-share-icon facebook">👍</div>
        <span>Share on Facebook</span>
      </a>
      <a href="#" class="main-share-option twitter-share" id="twitterShare">
        <div class="main-share-icon twitter">🐦</div>
        <span>Share on Twitter</span>
      </a>
      <button class="main-share-option copy-link" id="copyLink">
        <div class="main-share-icon copy">🔗</div>
        <span>Copy Link</span>
      </button>

      
    </div>

    
  </div>

  
</div>



{/* Toast Notification */}
<div class="main-toast" id="toast">
  <span class="main-toast-icon">✓</span>
  <span>Link copied to clipboard!</span>
</div>



<style>
  /* ===== CSS VARIABLES ===== */

  .main-top{
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
  }

  .main-call{
    border:none;
    text-decoration: none;
    background:white;
    font-size: 13px;


  }
  :root {
    --main-primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --main-success-gradient: linear-gradient(135deg, #10b981 0%, #059669 100%);
    --main-whatsapp-gradient: linear-gradient(135deg, #25D366 0%, #20ba5a 100%);
    --main-surface-light: #ffffff;
    --main-surface-gray: #f8fafc;
    --main-text-primary: #0f172a;
    --main-text-secondary: #64748b;
    --main-border-light: #e2e8f0;
    --main-shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.05);
    --main-shadow-md: 0 4px 12px rgba(0, 0, 0, 0.08);
    --main-shadow-lg: 0 10px 30px rgba(0, 0, 0, 0.12);
    --main-shadow-xl: 0 20px 40px rgba(0, 0, 0, 0.15);
    --main-radius-sm: 8px;
    --main-radius-md: 12px;
    --main-radius-lg: 16px;
    --main-radius-xl: 20px;
    --main-transition-smooth: cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* ===== GLOBAL RESET ===== */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    background: var(--main-surface-gray);
    display: flex!important;
    flex-direction: column!important;
    justify-content: flex-start!important;
    align-items: center!important;
    width:100vw!important;
    overflow-x: hidden!important;
  }

  .main-product-page-container {
    max-width: 1400px;
    width: 100%;
    margin: 0 auto;
    padding: 16px 16px 120px;
  }

  .main-product-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }

  /* ===== IMAGE CAROUSEL ===== */
  .main-image-section {
    position: relative;
    width: 100%;
  }

  .main-image-carousel {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    background: var(--main-surface-light);
    border-radius: var(--main-radius-xl);
    overflow: hidden;
    box-shadow: var(--main-shadow-lg);
    border: 1px solid var(--main-border-light);
  }

  .main-carousel-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 0.6s var(--main-transition-smooth);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .main-carousel-image.active {
    opacity: 1;
    animation: mainFadeIn 0.6s var(--main-transition-smooth);
  }

  @keyframes mainFadeIn {
    from {
      opacity: 0;
      transform: scale(0.98);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .main-product-image {
    width: 100%;
    height: 100%;
    object-fit: contain;
    padding: 20px;
  }

  .main-carousel-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(0, 0, 0, 0.08);
    width: 44px;
    height: 44px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    box-shadow: var(--main-shadow-md);
    transition: all 0.3s var(--main-transition-smooth);
    z-index: 10;
  }

  .main-carousel-btn:hover {
    background: white;
    transform: translateY(-50%) scale(1.1);
    box-shadow: var(--main-shadow-lg);
  }

  .main-carousel-btn:active {
    transform: translateY(-50%) scale(0.95);
  }

  .main-carousel-btn .main-arrow-icon {
    font-size: 32px;
    line-height: 1;
    color: var(--main-text-primary);
    font-weight: 300;
  }

  .main-carousel-btn.prev {
    left: 12px;
  }

  .main-carousel-btn.next {
    right: 12px;
  }

  .main-carousel-indicators {
    position: absolute;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 8px;
    z-index: 10;
    background: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(12px);
    padding: 8px 12px;
    border-radius: 50px;
  }

  .main-indicator {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
    border: none;
    cursor: pointer;
    transition: all 0.3s var(--main-transition-smooth);
    padding: 0;
  }

  .main-indicator.active {
    background: white;
    width: 20px;
    border-radius: 3px;
  }

  /* ===== TRUST BADGES MOBILE ===== */
  .main-trust-badges-mobile {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
    margin-top: 16px;
  }

  .main-trust-badge {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 16px;
    background: var(--main-surface-light);
    border-radius: var(--main-radius-md);
    border: 1px solid var(--main-border-light);
    box-shadow: var(--main-shadow-sm);
    transition: all 0.3s var(--main-transition-smooth);
  }

  .main-trust-badge:hover {
    transform: translateY(-2px);
    box-shadow: var(--main-shadow-md);
  }

  .main-badge-icon {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
    border-radius: var(--main-radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 20px;
  }

  .main-trust-badge span {
    font-size: 14px;
    font-weight: 600;
    color: var(--main-text-primary);
  }

  /* ===== DETAILS SECTION ===== */
  .main-details-section {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .main-product-header {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .main-product-title {
    font-size: 24px;
    font-weight: 800;
    color: var(--main-text-primary);
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .main-payment-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .main-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 14px;
    border-radius: 50px;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.01em;
  }

  .main-badge-icon-inline {
    font-size: 14px;
  }

  .main-badge-primary {
    background: linear-gradient(135deg, #dcfce7 0%, #bbf7d0 100%);
    color: #166534;
  }

  .main-badge-secondary {
    background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
    color: #92400e;
  }

  /* ===== MODERN PRICING CARD ===== */
  .main-pricing-card-modern {
    position: relative;
    background: var(--main-surface-light);
    border-radius: var(--main-radius-xl);
    padding: 24px;
    border: 1px solid var(--main-border-light);
    box-shadow: var(--main-shadow-lg);
    overflow: hidden;
  }

  .main-pricing-glow {
    position: absolute;
    top: -50%;
    right: -50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(102, 126, 234, 0.15) 0%, transparent 70%);
    pointer-events: none;
  }

  .main-pricing-content {
    position: relative;
    z-index: 1;
  }

  .main-pricing-header {
    margin-bottom: 20px;
  }

  .main-pricing-header h2 {
    font-size: 20px;
    font-weight: 800;
    color: var(--main-text-primary);
    margin-bottom: 6px;
    letter-spacing: -0.02em;
  }

  .main-pricing-subtitle {
    font-size: 13px;
    color: var(--main-text-secondary);
    font-weight: 500;
  }

  .main-pricing-options {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: 18px;
  }

  .main-pricing-option {
    background: var(--main-surface-gray);
    border-radius: var(--main-radius-md);
    padding: 18px;
    border: 2px solid transparent;
    transition: all 0.3s var(--main-transition-smooth);
  }

  .main-pricing-option:hover {
    transform: translateY(-2px);
    box-shadow: var(--main-shadow-md);
  }

  .main-pricing-option.deposit {
    background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
    border-color: #bae6fd;
  }

  .main-pricing-option.daily {
    background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
    border-color: #bbf7d0;
  }

  .main-option-label {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }

  .main-label-text {
    font-size: 12px;
    font-weight: 700;
    color: var(--main-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .main-label-badge {
    font-size: 10px;
    font-weight: 700;
    padding: 4px 10px;
    background: rgba(0, 0, 0, 0.06);
    border-radius: 50px;
    color: var(--main-text-secondary);
  }

  .main-option-amount {
    display: flex;
    align-items: baseline;
    gap: 4px;
  }

  .main-currency {
    font-size: 14px;
    font-weight: 700;
    color: var(--main-text-secondary);
  }

  .main-amount {
    font-size: 28px;
    font-weight: 900;
    color: var(--main-text-primary);
    letter-spacing: -0.03em;
  }

  .main-period {
    font-size: 13px;
    font-weight: 600;
    color: var(--main-text-secondary);
  }

  .main-pricing-separator {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 0 8px;
  }

  .main-separator-line {
    flex: 1;
    height: 1px;
    background: var(--main-border-light);
  }

  .main-separator-text {
    font-size: 11px;
    font-weight: 700;
    color: var(--main-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .main-pricing-benefits {
    display: flex;
    flex-direction: column;
    gap: 10px;
    padding-top: 18px;
    border-top: 1px solid var(--main-border-light);
  }

  .main-benefit-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 13px;
    font-weight: 600;
    color: var(--main-text-primary);
  }

  .main-check-icon {
    width: 20px;
    height: 20px;
    background: linear-gradient(135d, #d1fae5 0%, #a7f3d0 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 14px;
    font-weight: 900;
    color: #059669;
  }

  /* ===== CTA SECTION ===== */
  .main-cta-section {
    display: flex;
    gap: 10px;
  }

  .main-cta-button-main {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background: var(--main-whatsapp-gradient);
    color: white;
    padding: 16px 24px;
    border-radius: var(--main-radius-md);
    font-size: 15px;
    font-weight: 700;
    text-decoration: none;
    box-shadow: 0 4px 16px rgba(37, 211, 102, 0.35);
    transition: all 0.3s var(--main-transition-smooth);
    border: none;
  }

  .main-cta-button-main:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(37, 211, 102, 0.45);
  }

  .main-cta-button-main:active {
    transform: translateY(0);
  }

  .main-whatsapp-icon {
    font-size: 20px;
  }

  .main-arrow-icon {
    font-size: 18px;
  }

  .main-share-button {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    background: transparent;
    color: var(--main-text-primary);
    padding: 16px 20px;
    border-radius: var(--main-radius-md);
    font-size: 14px;
    font-weight: 700;
    border:none;
    cursor: pointer;
    transition: all 0.3s var(--main-transition-smooth);
    box-shadow: var(--main-shadow-sm);
  }

  .main-share-button:hover {
    background: var(--main-surface-gray);
    transform: translateY(-2px);
    box-shadow: var(--main-shadow-md);
  }

  .main-share-icon {
    font-size: 16px;
  }

  /* ===== HOW IT WORKS ===== */
  .main-how-it-works {
    background: var(--main-surface-light);
    border-radius: var(--main-radius-lg);
    padding: 20px;
    border: 1px solid var(--main-border-light);
    box-shadow: var(--main-shadow-sm);
  }

  .main-section-title {
    font-size: 18px;
    font-weight: 800;
    color: var(--main-text-primary);
    margin-bottom: 16px;
    letter-spacing: -0.01em;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .main-title-decoration {
    width: 4px;
    height: 20px;
    background: var(--main-primary-gradient);
    border-radius: 2px;
  }

  .main-steps-container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
  }

  .main-step-card {
    background: var(--main-surface-gray);
    border-radius: var(--main-radius-md);
    padding: 16px;
    border: 1px solid var(--main-border-light);
    transition: all 0.3s var(--main-transition-smooth);
  }

  .main-step-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--main-shadow-md);
    border-color: #cbd5e1;
  }

  .main-step-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 8px;
  }

  .main-step-number {
    width: 32px;
    height: 32px;
    background: var(--main-primary-gradient);
    color: white;
    border-radius: var(--main-radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    font-weight: 800;
    flex-shrink: 0;
  }

  .main-step-header h4 {
    font-size: 14px;
    font-weight: 700;
    color: var(--main-text-primary);
  }

  .main-step-card p {
    font-size: 13px;
    color: var(--main-text-secondary);
    line-height: 1.5;
    margin-left: 44px;
  }

  /* ===== PRODUCT DESCRIPTION ===== */
  .main-product-description {
    background: var(--main-surface-light);
    border-radius: var(--main-radius-lg);
    padding: 20px;
    border: 1px solid var(--main-border-light);
    box-shadow: var(--main-shadow-sm);
  }

  .main-description-text {
    font-size: 14px;
    line-height: 1.7;
    color: var(--main-text-secondary);
  }

  /* ===== FEATURES ===== */
  .main-product-features {
    background: var(--main-surface-light);
    border-radius: var(--main-radius-lg);
    padding: 20px;
    border: 1px solid var(--main-border-light);
    box-shadow: var(--main-shadow-sm);
  }

  .main-features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
  }

  .main-feature-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 12px;
    background: var(--main-surface-gray);
    border-radius: var(--main-radius-sm);
    transition: all 0.2s;
  }

  .main-feature-item:hover {
    background: #f1f5f9;
    transform: translateX(4px);
  }

  .main-feature-icon {
    width: 28px;
    height: 28px;
    background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 16px;
    font-weight: 900;
    color: #059669;
  }

  .main-feature-item span {
    font-size: 13px;
    color: var(--main-text-primary);
    line-height: 1.6;
    font-weight: 500;
    padding-top: 4px;
  }

  /* ===== DELIVERY CARD ===== */
  .main-delivery-card {
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
    border: 1px solid #bfdbfe;
    border-radius: var(--main-radius-lg);
    padding: 20px;
    display: flex;
    gap: 16px;
  }

  .main-delivery-icon {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
    border-radius: var(--main-radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 24px;
  }

  .main-delivery-content h4 {
    font-size: 16px;
    font-weight: 800;
    color: #1e40af;
    margin-bottom: 10px;
  }

  .main-delivery-content p {
    font-size: 13px;
    color: #1e40af;
    line-height: 1.6;
    margin-bottom: 6px;
  }

  .main-delivery-content strong {
    font-weight: 700;
  }

  .main-delivery-highlight {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 12px;
    padding-top: 12px;
    border-top: 1px solid #bfdbfe;
    font-size: 13px;
    font-weight: 700;
    color: #1e40af;
  }

  /* ===== TRUST SECTION ===== */
  .main-trust-section {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
  }

  .main-trust-card {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px;
    background: var(--main-surface-light);
    border: 1px solid var(--main-border-light);
    border-radius: var(--main-radius-md);
    transition: all 0.3s var(--main-transition-smooth);
    box-shadow: var(--main-shadow-sm);
  }

  .main-trust-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--main-shadow-md);
  }

  .main-trust-icon-wrapper {
    width: 48px;
    height: 48px;
    background: var(--main-primary-gradient);
    border-radius: var(--main-radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 24px;
  }

  .main-trust-text h4 {
    font-size: 14px;
    font-weight: 700;
    color: var(--main-text-primary);
    margin-bottom: 2px;
  }

  .main-trust-text p {
    font-size: 12px;
    color: var(--main-text-secondary);
  }

  /* ===== MOBILE STICKY CTA ===== */
  .main-mobile-sticky-cta {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(20px);
    border-top: 1px solid var(--main-border-light);
    padding: 12px 16px;
    box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.08);
    z-index: 1000;
    display: block;
  }

  .main-sticky-cta-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    max-width: 1400px;
    margin: 0 auto;
  }

  .main-sticky-price-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .main-sticky-deposit {
    font-size: 16px;
    font-weight: 900;
    color: var(--main-text-primary);
    letter-spacing: -0.02em;
  }

  .main-sticky-daily {
    font-size: 11px;
    font-weight: 600;
    color: var(--main-text-secondary);
  }

  .main-sticky-whatsapp-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    background: var(--main-whatsapp-gradient);
    color: white;
    padding: 12px 20px;
    border-radius: var(--main-radius-md);
    font-size: 14px;
    font-weight: 700;
    text-decoration: none;
    box-shadow: 0 4px 16px rgba(37, 211, 102, 0.35);
    transition: all 0.3s var(--main-transition-smooth);
    flex-shrink: 0;
  }

  .main-sticky-whatsapp-btn:active {
    transform: scale(0.96);
  }

  /* ===== SHARE MODAL ===== */
  .main-share-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 2000;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .main-share-modal.active {
    display: flex;
  }

  .main-share-modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
  }

  .main-share-modal-content {
    position: relative;
    background: var(--main-surface-light);
    border-radius: var(--main-radius-xl);
    padding: 24px;
    max-width: 420px;
    width: 100%;
    box-shadow: var(--main-shadow-xl);
    animation: mainModalSlideUp 0.3s var(--main-transition-smooth);
  }

  @keyframes mainModalSlideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .main-share-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }

  .main-share-modal-header h3 {
    font-size: 18px;
    font-weight: 800;
    color: var(--main-text-primary);
    letter-spacing: -0.02em;
  }

  .main-close-modal {
    background: var(--main-surface-gray);
    border: none;
    cursor: pointer;
    padding: 8px;
    border-radius: var(--main-radius-sm);
    color: var(--main-text-secondary);
    transition: all 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
  }

  .main-close-modal:hover {
    background: #e2e8f0;
    color: var(--main-text-primary);
  }

  .main-close-icon {
    font-size: 24px;
    line-height: 1;
  }

  .main-share-options {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .main-share-option {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    border-radius: var(--main-radius-md);
    border: 1px solid var(--main-border-light);
    text-decoration: none;
    color: var(--main-text-primary);
    font-size: 14px;
    font-weight: 600;
    transition: all 0.3s var(--main-transition-smooth);
    cursor: pointer;
    background: var(--main-surface-light);
  }

  .main-share-option:hover {
    background: var(--main-surface-gray);
    transform: translateX(4px);
    box-shadow: var(--main-shadow-sm);
  }

  .main-share-icon {
    width: 40px;
    height: 40px;
    border-radius: var(--main-radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 20px;
  }

  .main-share-icon.whatsapp {
    background: linear-gradient(135deg, #dcfce7 0%, #bbf7d0 100%);
  }

  .main-share-icon.facebook {
    background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
  }

  .main-share-icon.twitter {
    background: linear-gradient(135deg, #dbeafe 0%, #bae6fd 100%);
  }

  .main-share-icon.copy {
    background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
  }

  /* ===== TOAST ===== */
  .main-toast {
    position: fixed;
    bottom: 120px;
    left: 50%;
    transform: translateX(-50%) translateY(100px);
    background: var(--main-text-primary);
    color: white;
    padding: 12px 20px;
    border-radius: 50px;
    display: flex;
    align-items: center;
    gap: 10px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    opacity: 0;
    transition: all 0.4s var(--main-transition-smooth);
    z-index: 3000;
    font-size: 14px;
    font-weight: 600;
  }

  .main-toast.show {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }

  .main-toast-icon {
    font-size: 16px;
  }

  /* ===== RESPONSIVE TABLET ===== */
  @media (min-width: 640px) {
    .main-product-page-container {
      padding: 20px 20px 120px;
    }

    .main-product-grid {
      gap: 24px;
    }

    .main-product-title {
      font-size: 28px;
    }

    .main-pricing-card-modern {
      padding: 28px;
    }

    .main-amount {
      font-size: 32px;
    }

    .main-steps-container {
      grid-template-columns: repeat(2, 1fr);
      gap: 14px;
    }

    .main-features-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
    }

    .main-trust-badges-mobile {
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
    }
  }

  /* ===== RESPONSIVE DESKTOP ===== */
  @media (min-width: 768px) {
    .main-product-page-container {
      padding: 32px 24px;
    }

    .main-product-grid {
      grid-template-columns: 1fr 1fr;
      gap: 40px;
    }

    .main-trust-badges-mobile {
      display: none;
    }

    .main-mobile-sticky-cta {
      display: none;
    }

    .desktop-only {
      display: block;
    }

    .main-product-title {
      font-size: 32px;
    }

    .main-pricing-header h2 {
      font-size: 22px;
    }

    .main-amount {
      font-size: 36px;
    }

    .main-image-carousel {
      position: sticky;
      top: 24px;
    }

    .main-delivery-card {
      padding: 24px;
    }

    .main-trust-section {
      grid-template-columns: 1fr;
    }

    .main-carousel-btn {
      width: 48px;
      height: 48px;
    }

    .main-carousel-btn .main-arrow-icon {
      font-size: 36px;
    }

    .main-product-image {
      padding: 32px;
    }
  }

  @media (min-width: 1024px) {
    .main-product-grid {
      grid-template-columns: 560px 1fr;
      gap: 48px;
    }

    .main-details-section {
      gap: 24px;
    }

    .main-steps-container {
      grid-template-columns: repeat(2, 1fr);
      gap: 16px;
    }
  }

  @media (max-width: 767px) {
    .desktop-only {
      display: none;
    }
  }
</style>




<script>
  // Auto-Carousel with manual controls
  const carouselImages = document.querySelectorAll('.main-carousel-image');
  const indicators = document.querySelectorAll('.main-indicator');
  const prevBtn = document.querySelector('.main-carousel-btn.prev');
  const nextBtn = document.querySelector('.main-carousel-btn.next');
  
  let currentIndex = 0;
  let autoplayInterval: number | null = null;

  function showImage(index: number) {
    carouselImages.forEach((img, i) => {
      img.classList.toggle('active', i === index);
    });
    indicators.forEach((ind, i) => {
      ind.classList.toggle('active', i === index);
    });
  }

  function nextImage() {
    currentIndex = (currentIndex + 1) % carouselImages.length;
    showImage(currentIndex);
  }

  function prevImage() {
    currentIndex = (currentIndex - 1 + carouselImages.length) % carouselImages.length;
    showImage(currentIndex);
  }

  function startAutoplay() {
    if (carouselImages.length > 1) {
      autoplayInterval = window.setInterval(nextImage, 3000);
    }
  }

  function stopAutoplay() {
    if (autoplayInterval) {
      clearInterval(autoplayInterval);
      autoplayInterval = null;
    }
  }

  function resetAutoplay() {
    stopAutoplay();
    startAutoplay();
  }

  // Manual controls
  if (prevBtn && nextBtn) {
    prevBtn.addEventListener('click', () => {
      prevImage();
      resetAutoplay();
    });

    nextBtn.addEventListener('click', () => {
      nextImage();
      resetAutoplay();
    });
  }

  indicators.forEach((indicator, index) => {
    indicator.addEventListener('click', () => {
      currentIndex = index;
      showImage(currentIndex);
      resetAutoplay();
    });
  });

  // Pause on hover, resume on leave
  const carousel = document.querySelector('.main-image-carousel');
  if (carousel) {
    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
  }

  // Start autoplay on page load
  startAutoplay();

  // Share Modal
  const shareBtn = document.getElementById('shareBtn');
  const shareBtn2 = document.getElementById('shareBtn2');
  const shareModal = document.getElementById('shareModal');
  const closeModal = document.getElementById('closeModal');
  const copyLinkBtn = document.getElementById('copyLink');
  const toast = document.getElementById('toast');
  const modalOverlay = document.querySelector('.main-share-modal-overlay');

  if (shareBtn) {
    shareBtn.addEventListener('click', () => {
      shareModal?.classList.add('active');
    });
  }

  if (shareBtn2) {
    shareBtn2.addEventListener('click', () => {
      shareModal?.classList.add('active');
    });
  }

  if (closeModal) {
    closeModal.addEventListener('click', () => {
      shareModal?.classList.remove('active');
    });
  }

  if (modalOverlay) {
    modalOverlay.addEventListener('click', () => {
      shareModal?.classList.remove('active');
    });
  }

  // Share Options
  const whatsappShare = document.getElementById('whatsappShare');
  const facebookShare = document.getElementById('facebookShare');
  const twitterShare = document.getElementById('twitterShare');

  const pageUrl = encodeURIComponent(window.location.href);
  const pageTitle = encodeURIComponent(document.title);

  if (whatsappShare) {
    whatsappShare.setAttribute('href', `https://wa.me/?text=${pageTitle}%20${pageUrl}`);
  }

  if (facebookShare) {
    facebookShare.setAttribute('href', `https://www.facebook.com/sharer/sharer.php?u=${pageUrl}`);
  }

  if (twitterShare) {
    twitterShare.setAttribute('href', `https://twitter.com/intent/tweet?url=${pageUrl}&text=${pageTitle}`);
  }

  if (copyLinkBtn) {
    copyLinkBtn.addEventListener('click', async () => {
      try {
        await navigator.clipboard.writeText(window.location.href);
        toast?.classList.add('show');
        setTimeout(() => {
          toast?.classList.remove('show');
        }, 3000);
        shareModal?.classList.remove('active');
      } catch (err) {
        console.error('Failed to copy:', err);
      }
    });
  }
</script>



</body>
</html>