---
import { products } from '../data/product2.js';
import {
  ALL_BRANDS,
  DEPOSIT_FILTERS,
  STORAGE_FILTERS,
  brandSlug,
  parseDeposit,
} from '../data/filter-config.js';
import Gift from '../components/Gift.astro';
import Nav from '../components/Nav.astro';

const featured    = products.slice(0, 5);
const flashDeals  = products.filter(p => p.discount).slice(0, 4);
const budgetPicks = products.filter(p => parseDeposit(p.deposit) < 5000).slice(0, 5);
const samsungList = products.filter(p => p.brand?.toLowerCase() === 'samsung').slice(0, 5);
const tecnoList   = products.filter(p => p.brand?.toLowerCase() === 'tecno').slice(0, 5);
const infinixList = products.filter(p => p.brand?.toLowerCase() === 'infinix').slice(0, 5);

function renderStars(rating) {
  const full = Math.floor(rating ?? 4.5);
  return '★'.repeat(full) + '☆'.repeat(5 - full);
}

const pageTitle = 'MdogomdogoDeals — Lipa Mdogo Mdogo Phones Nairobi | Pay Weekly via M-Pesa';
const metaDesc  = 'Buy smartphones on installments in Nairobi. Pay a small deposit from KSh 2,000 and spread the rest weekly via M-Pesa. Samsung, Tecno, Infinix — no credit check, free delivery Nairobi CBD.';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{pageTitle}</title>
  <meta name="description" content={metaDesc} />
  <link rel="canonical" href="https://mdogomdogodeals.co.ke/" />
  <meta property="og:type"        content="website" />
  <meta property="og:title"       content={pageTitle} />
  <meta property="og:description" content={metaDesc} />
  <meta property="og:url"         content="https://mdogomdogodeals.co.ke/" />
  <meta property="og:site_name"   content="MdogomdogoDeals" />

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      /* — Core palette: ink + blue + one functional red — */
      --ink:        #0d1117;   /* headings, logo */
      --ink-mid:    #24292f;   /* strong text */
      --ink-light:  #57606a;   /* body / labels */
      --ink-faint:  #8c959f;   /* captions */

      --blue:       #1a56db;   /* primary CTA, links, active */
      --blue-dark:  #1341b0;   /* hover */
      --blue-bg:    #f0f4ff;   /* tinted backgrounds */
      --blue-line:  #c8d8fc;   /* borders / dividers */

      --red:        #cf222e;   /* badge-hot, flash bar */
      --red-bg:     #fff0f0;

      --canvas:     #ffffff;
      --surface:    #f6f8fa;   /* section bg, card hover */
      --border:     #d0d7de;   /* card / input borders */
      --border-mid: #e8ecef;   /* lighter rules */

      --radius-sm: 6px;
      --radius-md: 10px;
      --radius-lg: 14px;
      --shadow-sm: 0 1px 3px rgba(13,17,23,0.07);
      --shadow-md: 0 4px 16px rgba(13,17,23,0.09);
      --shadow-lg: 0 10px 32px rgba(13,17,23,0.12);
    }

    body { font-family: 'Inter', sans-serif; background: var(--canvas); color: var(--ink-mid); -webkit-font-smoothing: antialiased; }
    a { text-decoration: none; color: inherit; }
    img { display: block; }

    /* TOP BAR */
    .top-bar { background: var(--ink); color: #8c959f; font-size: 12px; font-weight: 500; padding: 7px 24px; display: flex; align-items: center; justify-content: space-between; gap: 12px; }
    .top-bar-pills { display: flex; align-items: center; gap: 6px; flex-wrap: wrap; }
    .top-pill { display: flex; align-items: center; gap: 5px; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1); border-radius: 20px; padding: 3px 10px; font-size: 11.5px; color: #c9d1d9; }
    .top-pill svg { width: 10px; height: 10px; color: #c9d1d9; }
    .top-bar-right { display: flex; align-items: center; gap: 10px; }
    .top-wa { background: #238636; color: #fff; border-radius: 6px; padding: 4px 12px; font-size: 11.5px; font-weight: 600; display: flex; align-items: center; gap: 5px; white-space: nowrap; }
    .top-wa svg { width: 12px; height: 12px; }

    /* SITE HEADER */
    .site-header { background: var(--canvas); border-bottom: 1px solid var(--border); padding: 13px 24px; display: flex; align-items: center; justify-content: space-between; gap: 16px; position: sticky; top: 0; z-index: 100; box-shadow: var(--shadow-sm); }
    .logo { font-size: 20px; font-weight: 800; color: var(--ink); letter-spacing: -0.5px; white-space: nowrap; }
    .logo em { color: var(--blue); font-style: normal; }
    .logo-badge { display: inline-block; background: var(--blue); color: #fff; font-size: 9px; font-weight: 700; padding: 2px 6px; border-radius: 4px; margin-left: 5px; letter-spacing: 0.05em; vertical-align: middle; text-transform: uppercase; }
    .header-search { flex: 1; max-width: 480px; display: flex; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-md); overflow: hidden; transition: border-color 0.15s, box-shadow 0.15s; }
    .header-search:focus-within { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(26,86,219,0.1); background: var(--canvas); }
    .header-search input { flex: 1; border: none; outline: none; padding: 10px 14px; font-size: 14px; font-family: 'Inter', sans-serif; color: var(--ink-mid); background: transparent; }
    .header-search input::placeholder { color: var(--ink-faint); }
    .header-search button { background: var(--blue); border: none; color: #fff; padding: 0 18px; cursor: pointer; font-size: 13px; font-family: 'Inter', sans-serif; font-weight: 600; white-space: nowrap; transition: background 0.15s; }
    .header-search button:hover { background: var(--blue-dark); }
    .header-right { display: flex; align-items: center; gap: 16px; }
    .header-contact .label { font-size: 10px; color: var(--ink-faint); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 1px; }
    .header-contact .number { font-size: 17px; font-weight: 800; color: var(--ink); letter-spacing: -0.3px; }

    /* MAIN NAV */
    .main-nav { background: var(--canvas); border-bottom: 1px solid var(--border); }
    .main-nav ul { max-width: 1320px; margin: 0 auto; padding: 0 24px; list-style: none; display: flex; align-items: center; overflow-x: auto; scrollbar-width: none; }
    .main-nav ul::-webkit-scrollbar { display: none; }
    .main-nav li a { display: block; padding: 12px 14px; font-size: 13px; font-weight: 500; color: var(--ink-light); border-bottom: 2px solid transparent; margin-bottom: -1px; white-space: nowrap; transition: color 0.15s, border-color 0.15s; }
    .main-nav li a:hover { color: var(--blue); border-bottom-color: var(--blue-line); }
    .main-nav li a.active { color: var(--blue); border-bottom-color: var(--blue); font-weight: 600; }

    /* HERO */
    .hero { background: linear-gradient(160deg, #0d1117 0%, #161c2d 55%, #1a3060 100%); padding: 48px 24px 44px; position: relative; overflow: hidden; }
    .hero::before { content: ''; position: absolute; inset: 0; background: radial-gradient(ellipse 700px 500px at 85% 50%, rgba(26,86,219,0.18) 0%, transparent 70%); pointer-events: none; }
    .hero-inner { max-width: 1320px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: center; position: relative; z-index: 1; }
    .hero-eyebrow { display: inline-flex; align-items: center; gap: 6px; background: rgba(255,255,255,0.07); border: 1px solid rgba(255,255,255,0.14); border-radius: 20px; padding: 4px 12px; font-size: 11.5px; font-weight: 600; color: #c9d1d9; letter-spacing: 0.04em; text-transform: uppercase; margin-bottom: 14px; }
    .hero-eyebrow svg { width: 10px; height: 10px; color: #c9d1d9; }
    .hero-text h1 { font-size: 42px; font-weight: 800; color: #fff; line-height: 1.1; margin-bottom: 16px; letter-spacing: -1px; }
    .hero-text h1 em { font-style: normal; color: #7eb6ff; }
    .hero-text p { font-size: 15px; color: #8c959f; margin-bottom: 28px; line-height: 1.7; max-width: 440px; }
    .hero-text p strong { color: #c9d1d9; font-weight: 600; }
    .hero-btns { display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 32px; }
    .btn-hero { background: var(--blue); color: #fff; border-radius: var(--radius-md); padding: 12px 28px; font-size: 14px; font-weight: 700; display: inline-flex; align-items: center; gap: 6px; transition: background 0.15s, transform 0.15s; letter-spacing: -0.2px; font-family: 'Inter', sans-serif; }
    .btn-hero:hover { background: var(--blue-dark); transform: translateY(-1px); }
    .btn-hero svg { width: 14px; height: 14px; }
    .btn-hero-ghost { background: rgba(255,255,255,0.08); color: #c9d1d9; border: 1px solid rgba(255,255,255,0.18); border-radius: var(--radius-md); padding: 11px 24px; font-size: 14px; font-weight: 600; font-family: 'Inter', sans-serif; display: inline-block; transition: background 0.15s; }
    .btn-hero-ghost:hover { background: rgba(255,255,255,0.13); }
    .hero-stats { display: flex; gap: 24px; flex-wrap: wrap; }
    .hero-stat .num { font-size: 26px; font-weight: 800; color: #fff; letter-spacing: -0.5px; }
    .hero-stat .lbl { font-size: 12px; color: #57606a; margin-top: 1px; }
    .hero-right { display: flex; flex-direction: column; gap: 12px; }
    .dep-cards { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    .dep-card { background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.12); border-radius: var(--radius-md); padding: 16px; text-align: center; transition: background 0.15s, border-color 0.15s; cursor: pointer; }
    .dep-card:hover { background: rgba(255,255,255,0.1); border-color: rgba(255,255,255,0.3); }
    .dep-card .amount { font-size: 20px; font-weight: 800; color: #fff; }
    .dep-card .sublabel { font-size: 11px; color: #57606a; margin-top: 3px; }
    .hero-badges { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .hero-badge { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: var(--radius-sm); padding: 10px 8px; text-align: center; }
    .hero-badge svg { width: 18px; height: 18px; color: #8c959f; margin: 0 auto 5px; }
    .hero-badge span { display: block; font-size: 11px; color: #8c959f; line-height: 1.4; font-weight: 500; }

    /* FLASH DEALS BAR */
    .flash-bar { background: var(--red); padding: 0 24px; display: flex; align-items: stretch; gap: 0; overflow: hidden; }
    .flash-label { background: #a40e26; color: #fff; font-size: 13px; font-weight: 700; padding: 10px 18px; display: flex; align-items: center; gap: 6px; white-space: nowrap; flex-shrink: 0; font-family: 'Inter', sans-serif; }
    .flash-label svg { width: 14px; height: 14px; animation: pulse 1s infinite; }
    @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:0.5} }
    .flash-items { display: flex; align-items: center; gap: 0; overflow-x: auto; scrollbar-width: none; flex: 1; }
    .flash-items::-webkit-scrollbar { display: none; }
    .flash-item { display: flex; align-items: center; gap: 10px; padding: 8px 16px; border-right: 1px solid rgba(255,255,255,0.15); white-space: nowrap; transition: background 0.15s; }
    .flash-item:hover { background: rgba(255,255,255,0.1); }
    .flash-item img { width: 36px; height: 36px; object-fit: contain; border-radius: 4px; background: rgba(255,255,255,0.15); }
    .flash-item-name { font-size: 12.5px; color: #fff; font-weight: 500; max-width: 120px; overflow: hidden; text-overflow: ellipsis; }
    .flash-item-price { font-size: 14px; font-weight: 700; color: #fff; }
    .flash-badge { background: rgba(255,255,255,0.2); color: #fff; font-size: 10px; font-weight: 700; padding: 2px 7px; border-radius: 3px; }

    /* TRUST BAR */
    /* TRUST BAR */
    .trust-bar { background: var(--canvas); border-bottom: 1px solid var(--border); padding: 12px 24px; }
    .trust-inner { max-width: 1320px; margin: 0 auto; display: flex; align-items: center; justify-content: center; flex-wrap: wrap; }
    .trust-item { display: flex; align-items: center; gap: 8px; font-size: 12.5px; font-weight: 500; color: var(--ink-light); padding: 0 24px; border-right: 1px solid var(--border-mid); }
    .trust-item:last-child { border-right: none; }
    .trust-icon { width: 26px; height: 26px; background: var(--blue-bg); border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
    .trust-icon svg { width: 12px; height: 12px; color: var(--blue); }

    /* PAGE WRAP */
    .page-wrap { max-width: 1320px; margin: 0 auto; padding: 0 24px; }

    /* SECTION HEADS */
    .section-head { display: flex; align-items: center; justify-content: space-between; padding-top: 32px; margin-bottom: 16px; }
    .section-head-left { display: flex; align-items: center; gap: 10px; }
    .section-head h2 { font-size: 19px; font-weight: 700; color: var(--ink); letter-spacing: -0.4px; }
    .section-rule { border: none; border-top: 1px solid var(--border-mid); margin: 4px 0 0; }
    .tag { display: inline-block; font-size: 10px; font-weight: 700; padding: 3px 8px; border-radius: 4px; letter-spacing: 0.04em; text-transform: uppercase; }
    .tag-blue  { background: var(--blue-bg); color: var(--blue); }
    .tag-red   { background: var(--red-bg); color: var(--red); }
    .tag-ink   { background: var(--surface); color: var(--ink-light); }
    .see-all { font-size: 13px; font-weight: 600; color: var(--blue); display: flex; align-items: center; gap: 4px; }
    .see-all:hover { text-decoration: underline; }
    .see-all svg { width: 14px; height: 14px; }

    /* BRAND GRID */
    .brand-grid { display: grid; grid-template-columns: repeat(8, 1fr); gap: 10px; margin-bottom: 8px; }
    .brand-chip { background: var(--canvas); border: 1px solid var(--border); border-radius: var(--radius-md); padding: 14px 8px 10px; text-align: center; transition: border-color 0.15s, box-shadow 0.15s, transform 0.15s; cursor: pointer; display: flex; flex-direction: column; align-items: center; justify-content: center; }
    .brand-chip:hover { border-color: var(--blue); box-shadow: var(--shadow-md); transform: translateY(-2px); }
    .brand-logo-wrap { display: flex; align-items: center; justify-content: center; height: 46px; margin-bottom: 6px; }
    .brand-chip-sub { font-size: 11px; color: var(--ink-faint); font-weight: 500; }

    /* PRODUCT CARD */
    .product-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 14px; margin-bottom: 8px; }
    .card { display: flex; flex-direction: column; background: var(--canvas); border-radius: var(--radius-lg); border: 1px solid var(--border); overflow: hidden; position: relative; transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s cubic-bezier(0.34,1.56,0.64,1); text-decoration: none; color: inherit; }
    .card:hover { border-color: var(--blue-line); box-shadow: var(--shadow-lg); transform: translateY(-4px); }
    .card-badge { position: absolute; top: 10px; left: 10px; z-index: 3; padding: 3px 8px; border-radius: 5px; font-size: 10px; font-weight: 700; letter-spacing: 0.05em; text-transform: uppercase; color: #fff; line-height: 1; }
    .badge-new  { background: #1a7f37; }
    .badge-hot  { background: var(--red); }
    .badge-sale { background: var(--blue); }
    .card-discount { position: absolute; top: 10px; right: 10px; z-index: 3; background: var(--red); color: #fff; font-size: 10.5px; font-weight: 700; padding: 3px 8px; border-radius: 5px; line-height: 1.4; }
    .card-img-wrap { width: 100%; aspect-ratio: 4/3; background: var(--surface); display: flex; align-items: center; justify-content: center; overflow: hidden; flex-shrink: 0; }
    .card-img { width: 85%; height: 85%; object-fit: contain; object-position: center; transition: transform 0.35s cubic-bezier(0.34,1.2,0.64,1); }
    .card:hover .card-img { transform: scale(1.06); }
    .card-body { padding: 13px 13px 11px; flex: 1; display: flex; flex-direction: column; }
    .card-brand { font-size: 10px; font-weight: 700; color: var(--blue); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 4px; }
    .card-name { font-size: 13.5px; font-weight: 500; color: var(--ink-mid); line-height: 1.4; margin-bottom: 8px; flex: 1; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
    .card-specs { display: flex; gap: 4px; flex-wrap: wrap; margin-bottom: 9px; }
    .spec-pill { background: var(--surface); border: 1px solid var(--border-mid); color: var(--ink-light); border-radius: 4px; padding: 2px 7px; font-size: 10.5px; font-weight: 500; white-space: nowrap; }
    .card-rating { display: flex; align-items: center; gap: 5px; margin-bottom: 10px; }
    .stars { color: #b08800; font-size: 12px; letter-spacing: 0.5px; }
    .rating-count { font-size: 11px; color: var(--ink-faint); }
    .card-price { margin-bottom: 6px; }
    .price-deposit { font-size: 23px; font-weight: 800; color: var(--blue); letter-spacing: -0.5px; line-height: 1; }
    .price-label { font-size: 11px; color: var(--ink-faint); font-weight: 400; margin-left: 3px; }
    .installment-note { font-size: 11px; color: #1a7f37; font-weight: 600; background: #f0faf4; padding: 4px 9px; border-radius: 5px; margin-bottom: 11px; display: inline-block; line-height: 1.5; }
    .card-cta { display: flex; gap: 6px; margin-top: auto; }
    .btn-cart { flex: 1; background: var(--blue); color: #fff; border: none; border-radius: var(--radius-sm); padding: 9px 8px; font-size: 12.5px; font-weight: 600; cursor: pointer; font-family: 'Inter', sans-serif; transition: background 0.15s; }
    .btn-cart:hover { background: var(--blue-dark); }
    .btn-buy { flex: 1; background: var(--ink); color: #fff; border: none; border-radius: var(--radius-sm); padding: 9px 8px; font-size: 12.5px; font-weight: 600; cursor: pointer; font-family: 'Inter', sans-serif; transition: background 0.15s; }
    .btn-buy:hover { background: #24292f; }

    /* BUDGET PICKS */
    .budget-section { background: var(--ink); border-radius: var(--radius-lg); padding: 28px 24px; margin: 28px 0; }
    .budget-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 18px; }
    .budget-head h2 { font-size: 19px; font-weight: 700; color: #fff; }
    .budget-head h2 span { color: #7eb6ff; }
    .budget-see-all { font-size: 12.5px; color: #57606a; font-weight: 500; }
    .budget-see-all:hover { color: #8c959f; }
    .budget-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 10px; }
    .budget-card { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: var(--radius-md); overflow: hidden; transition: background 0.15s, border-color 0.15s, transform 0.2s cubic-bezier(0.34,1.3,0.64,1); text-decoration: none; color: inherit; display: flex; flex-direction: column; }
    .budget-card:hover { background: rgba(255,255,255,0.09); border-color: rgba(255,255,255,0.25); transform: translateY(-3px); }
    .budget-img-wrap { background: rgba(255,255,255,0.07); aspect-ratio: 4/3; display: flex; align-items: center; justify-content: center; overflow: hidden; }
    .budget-img { width: 80%; height: 80%; object-fit: contain; transition: transform 0.3s; }
    .budget-card:hover .budget-img { transform: scale(1.06); }
    .budget-body { padding: 10px 11px; flex: 1; display: flex; flex-direction: column; }
    .budget-name { font-size: 12px; font-weight: 500; color: #c9d1d9; line-height: 1.4; margin-bottom: 7px; flex: 1; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
    .budget-deposit { font-size: 18px; font-weight: 800; color: #7eb6ff; }
    .budget-deposit-lbl { font-size: 10px; color: #57606a; }
    .budget-cta { width: 100%; background: rgba(126,182,255,0.1); border: 1px solid rgba(126,182,255,0.2); color: #7eb6ff; padding: 8px; font-size: 12px; font-weight: 600; cursor: pointer; font-family: 'Inter', sans-serif; transition: background 0.15s; text-align: center; display: block; }
    .budget-cta:hover { background: rgba(126,182,255,0.18); }

    /* PROMO BANNERS */
    .promo-banners { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin: 28px 0; }
    .promo-banner { border-radius: var(--radius-lg); padding: 24px 28px; display: flex; align-items: center; justify-content: space-between; gap: 16px; overflow: hidden; position: relative; }
    .promo-banner-a { background: linear-gradient(135deg, #161c2d 0%, #1e3a6e 100%); border: 1px solid rgba(26,86,219,0.3); }
    .promo-banner-b { background: var(--ink); border: 1px solid rgba(255,255,255,0.1); }
    .promo-text h3 { font-size: 16px; font-weight: 700; color: #fff; margin-bottom: 6px; letter-spacing: -0.3px; }
    .promo-text p { font-size: 13px; color: rgba(255,255,255,0.6); line-height: 1.5; }
    .promo-badge { background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: var(--radius-md); padding: 12px 16px; text-align: center; flex-shrink: 0; }
    .promo-badge .big { font-size: 22px; font-weight: 800; color: #fff; line-height: 1; }
    .promo-badge .small { font-size: 10px; color: rgba(255,255,255,0.6); margin-top: 3px; }
    .promo-btn { display: inline-block; margin-top: 14px; background: rgba(255,255,255,0.15); color: #fff; border-radius: 6px; padding: 7px 16px; font-size: 12.5px; font-weight: 600; transition: background 0.15s; }
    .promo-btn:hover { background: rgba(255,255,255,0.22); }

    /* HOW IT WORKS */
    .how-section { background: var(--surface); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); padding: 36px 24px; }
    .how-inner { max-width: 1320px; margin: 0 auto; }
    .how-header { text-align: center; margin-bottom: 28px; }
    .how-header h2 { font-size: 22px; font-weight: 700; color: var(--ink); margin-bottom: 6px; letter-spacing: -0.4px; }
    .how-header p { font-size: 14px; color: var(--ink-light); }
    .how-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; position: relative; }
    .how-grid::before { content: ''; position: absolute; top: 22px; left: calc(12.5% + 17px); right: calc(12.5% + 17px); height: 2px; background: linear-gradient(90deg, var(--blue-line) 0%, var(--blue) 100%); z-index: 0; }
    .how-card { background: var(--canvas); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 20px 16px 16px; text-align: center; position: relative; z-index: 1; transition: box-shadow 0.15s, transform 0.15s; }
    .how-card:hover { box-shadow: var(--shadow-md); transform: translateY(-2px); }
    .how-num { width: 44px; height: 44px; border-radius: 10px; font-size: 20px; font-weight: 800; display: flex; align-items: center; justify-content: center; margin: 0 auto 12px; }
    .how-num-1 { background: var(--blue); color: #fff; }
    .how-num-2 { background: var(--blue-bg); color: var(--blue); border: 1px solid var(--blue-line); }
    .how-num-3 { background: var(--blue-bg); color: var(--blue); border: 1px solid var(--blue-line); }
    .how-num-4 { background: var(--ink); color: #fff; }
    .how-title { font-size: 13.5px; font-weight: 700; color: var(--ink); margin-bottom: 6px; }
    .how-desc { font-size: 12.5px; color: var(--ink-light); line-height: 1.65; }

    /* WHY */
    .why-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 32px; }
    .why-card { background: var(--canvas); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 20px 16px; transition: box-shadow 0.15s, transform 0.15s; }
    .why-card:hover { box-shadow: var(--shadow-md); transform: translateY(-2px); }
    .why-icon-wrap { width: 38px; height: 38px; background: var(--blue-bg); border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center; margin-bottom: 12px; }
    .why-icon-wrap svg { width: 17px; height: 17px; color: var(--blue); }
    .why-title { font-size: 13.5px; font-weight: 700; color: var(--ink); margin-bottom: 5px; }
    .why-desc { font-size: 12.5px; color: var(--ink-light); line-height: 1.6; }

    /* TESTIMONIALS */
    .testimonials-section { padding: 36px 24px; background: var(--surface); border-top: 1px solid var(--border); }
    .testimonials-inner { max-width: 1320px; margin: 0 auto; }
    .testimonials-head { text-align: center; margin-bottom: 24px; }
    .testimonials-head h2 { font-size: 21px; font-weight: 700; color: var(--ink); margin-bottom: 4px; }
    .testimonials-head p { font-size: 14px; color: var(--ink-light); }
    .testimonials-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
    .testi-card { background: var(--canvas); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 20px; }
    .testi-stars { color: #b08800; font-size: 14px; margin-bottom: 10px; }
    .testi-text { font-size: 13.5px; color: var(--ink-mid); line-height: 1.65; margin-bottom: 14px; font-style: italic; }
    .testi-footer { display: flex; align-items: center; gap: 10px; }
    .testi-avatar { width: 36px; height: 36px; border-radius: 50%; background: var(--blue-bg); display: flex; align-items: center; justify-content: center; font-size: 13px; font-weight: 700; color: var(--blue); flex-shrink: 0; }
    .testi-name { font-size: 13px; font-weight: 600; color: var(--ink); }
    .testi-loc { font-size: 11px; color: var(--ink-faint); }

    /* FAQ */
    .faq-section { padding: 36px 24px; }
    .faq-inner { max-width: 800px; margin: 0 auto; }
    .faq-head { text-align: center; margin-bottom: 24px; }
    .faq-head h2 { font-size: 21px; font-weight: 700; color: var(--ink); margin-bottom: 4px; }
    .faq-head p { font-size: 14px; color: var(--ink-light); }
    .faq-item { border: 1px solid var(--border); border-radius: var(--radius-md); margin-bottom: 8px; overflow: hidden; }
    .faq-q { width: 100%; background: var(--canvas); border: none; padding: 16px 18px; display: flex; align-items: center; justify-content: space-between; gap: 12px; cursor: pointer; font-size: 14px; font-weight: 600; color: var(--ink-mid); font-family: 'Inter', sans-serif; text-align: left; transition: background 0.15s; }
    .faq-q:hover { background: var(--surface); }
    .faq-q.open { color: var(--blue); border-bottom: 1px solid var(--border-mid); }
    .faq-q svg { width: 16px; height: 16px; flex-shrink: 0; transition: transform 0.2s; color: var(--ink-faint); }
    .faq-q.open svg { transform: rotate(45deg); color: var(--blue); }
    .faq-a { display: none; padding: 14px 18px; font-size: 13.5px; color: var(--ink-light); line-height: 1.7; background: var(--surface); }
    .faq-a.open { display: block; }
    .faq-a a { color: var(--blue); font-weight: 500; }
    .faq-a a:hover { text-decoration: underline; }

    /* SEO CONTENT */
    .seo-content { border-top: 1px solid var(--border-mid); padding: 32px 24px; background: var(--canvas); }
    .seo-content-inner { max-width: 900px; margin: 0 auto; }
    .seo-content h2 { font-size: 17px; font-weight: 700; color: var(--ink); margin-bottom: 10px; }
    .seo-content p { font-size: 13.5px; color: var(--ink-light); line-height: 1.8; margin-bottom: 10px; }
    .seo-content a { color: var(--blue); font-weight: 500; }
    .seo-content a:hover { text-decoration: underline; }

    /* FOOTER */
    .footer { background: var(--ink); padding: 40px 24px 20px; }
    .footer-inner { max-width: 1320px; margin: 0 auto; }
    .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 36px; margin-bottom: 28px; }
    .footer-logo { font-size: 20px; font-weight: 800; color: #fff; margin-bottom: 10px; display: block; }
    .footer-logo em { color: #7eb6ff; font-style: normal; }
    .footer-tagline { font-size: 13px; color: #57606a; line-height: 1.7; margin-bottom: 16px; }
    .footer-contact a { display: block; font-size: 13px; color: #8c959f; margin-bottom: 5px; }
    .footer-contact a:hover { color: #c9d1d9; }
    .footer-col-title { font-size: 10px; font-weight: 700; color: #444c56; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 12px; }
    .footer-links { list-style: none; }
    .footer-links li { margin-bottom: 7px; }
    .footer-links a { font-size: 13px; color: #57606a; transition: color 0.15s; }
    .footer-links a:hover { color: #c9d1d9; }
    .footer-bottom { border-top: 1px solid rgba(255,255,255,0.07); padding-top: 16px; display: flex; justify-content: space-between; flex-wrap: wrap; gap: 8px; }
    .footer-bottom p { font-size: 12px; color: #444c56; }

    /* SEO TAGS */
    .seo-tags { border-top: 1px solid rgba(255,255,255,0.05); padding: 14px 24px; background: #090d12; }
    .seo-tags-inner { max-width: 1320px; margin: 0 auto; display: flex; flex-wrap: wrap; gap: 6px; }
    .seo-tag { font-size: 11.5px; color: #444c56; border: 1px solid #21262d; border-radius: 4px; padding: 3px 9px; background: #0d1117; transition: color 0.12s, border-color 0.12s; }
    .seo-tag:hover { color: #8c959f; border-color: #444c56; }

    /* RESPONSIVE */
    @media (max-width: 1100px) {
      .product-grid { grid-template-columns: repeat(4, 1fr); }
      .budget-grid { grid-template-columns: repeat(4, 1fr); }
      .brand-grid { grid-template-columns: repeat(4, 1fr); }
      .hero-inner { grid-template-columns: 1fr; }
      .hero-right { display: none; }
    }
    @media (max-width: 900px) {
      .product-grid { grid-template-columns: repeat(3, 1fr); }
      .budget-grid { grid-template-columns: repeat(3, 1fr); }
      .how-grid { grid-template-columns: repeat(2, 1fr); }
      .how-grid::before { display: none; }
      .why-grid { grid-template-columns: repeat(2, 1fr); }
      .footer-grid { grid-template-columns: 1fr 1fr; }
      .testimonials-grid { grid-template-columns: 1fr; }
      .promo-banners { grid-template-columns: 1fr; }
    }
    @media (max-width: 700px) {
      .product-grid { grid-template-columns: repeat(2, 1fr); }
      .budget-grid { grid-template-columns: repeat(2, 1fr); }
      .hero-text h1 { font-size: 30px; }
    }
    @media (max-width: 500px) {
      .header-search { display: none; }
      .brand-grid { grid-template-columns: repeat(4, 1fr); }
      .how-grid { grid-template-columns: 1fr; }
      .footer-grid { grid-template-columns: 1fr; }
      .trust-item { padding: 0 12px; border: none; }
    }
  </style>
</head>
<body>

  <Gift />
  <Nav />

  <!-- TOP BAR -->
  <div class="top-bar" role="banner" aria-label="Site announcements">
    <div class="top-bar-pills">
      <span class="top-pill">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        Free delivery 
      </span>
      <span class="top-pill">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        No credit check
      </span>
      <span class="top-pill">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        Pay after delivery via M-Pesa
      </span>
    </div>
    <div class="top-bar-right">
      <span style="font-size:11.5px;color:#64748b;">Call: <strong style="color:#e2e8f0;">0100028823</strong></span>
      <a href="https://wa.me/254100028823" class="top-wa">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
        WhatsApp Us
      </a>
    </div>
  </div>

  <!-- SITE HEADER -->
  <!-- <header class="site-header">
    <a href="/" class="logo">Mdogo<em>mdogo</em>Deals <span class="logo-badge">Nairobi</span></a>
    <div class="header-search" role="search">
      <input type="search" placeholder="Search phones, brands, models…" aria-label="Search phones" />
      <button type="submit" aria-label="Search">Search</button>
    </div>
    <div class="header-right">
      <div class="header-contact">
        <div class="label">Order Now</div>
        <a href="tel:+254100028823" class="number">0100 028 823</a>
      </div>
    </div>
  </header> -->

  <!-- MAIN NAV -->
  <nav class="main-nav" aria-label="Main navigation">
    <ul>
      <li><a href="/" class="active">Home</a></li>
      <li><a href="/phones/lipa-mdogo-mdogo-nairobi">All Phones</a></li>
      {ALL_BRANDS.map(brand => (
        <li><a href={`/phones/${brandSlug(brand)}`}>{brand}</a></li>
      ))}
      {DEPOSIT_FILTERS.map(dep => (
        <li><a href={`/phones/${dep.key}`}>{dep.label}</a></li>
      ))}
    </ul>
  </nav>

  <!-- HERO -->
  <section class="hero" aria-label="Hero">
    <div class="hero-inner">
      <div class="hero-text">
        <div class="hero-eyebrow">
          <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
          Nairobi's #1 Installment Phone Shop
        </div>
        <h1>
          <em>Lipa Mdogo Mdogo</em><br>
          Phones Nairobi
        </h1>
        <p>
          Pay a small deposit from <strong>KSh 2,000</strong> and take your phone home today.
          Spread the rest in <strong>daily, weekly or monthly</strong> M-Pesa installments.
          No credit check. No guarantor. No stress.
        </p>
        <div class="hero-btns">
          <a href="/phones/lipa-mdogo-mdogo-nairobi" class="btn-hero">
            Browse All Phones
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
          </a>
          <a href="/phones/deposit-under-5000" class="btn-hero-ghost">Under KSh 5K Deposit</a>
        </div>
        <div class="hero-stats">
          <div class="hero-stat">
            <div class="num">{products.length}+</div>
            <div class="lbl">Phones Available</div>
          </div>
          <div class="hero-stat">
            <div class="num">KSh 2K</div>
            <div class="lbl">Min. Deposit</div>
          </div>
          <div class="hero-stat">
            <div class="num">Same Day</div>
            <div class="lbl">Nairobi Delivery</div>
          </div>
          <div class="hero-stat">
            <div class="num">0%</div>
            <div class="lbl">Credit Checks</div>
          </div>
        </div>
      </div>

      <div class="hero-right">
        <div class="dep-cards">
          {DEPOSIT_FILTERS.slice(0, 4).map(dep => (
            <a href={`/phones/${dep.key}`} class="dep-card">
              <div class="amount">{dep.label}</div>
              <div class="sublabel">Deposit Range</div>
            </a>
          ))}
        </div>
        <div class="hero-badges">
          <div class="hero-badge">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
            <span>100% Genuine</span>
          </div>
          <div class="hero-badge">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg>
            <span>M-Pesa Payments</span>
          </div>
          <div class="hero-badge">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12H3l9-9 9 9h-2"/><path d="M5 12v7a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-7"/></svg>
            <span>Free Delivery</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FLASH DEALS BAR -->
  {flashDeals.length > 0 && (
    <div class="flash-bar" role="region" aria-label="Flash deals">
      <div class="flash-label">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
        Flash Deals
      </div>
      <div class="flash-items">
        {flashDeals.map(p => (
          <a href={`/${p.slug}`} class="flash-item">
            <img src={p.source} alt={p.fullname ?? p.name} width="36" height="36" loading="lazy" />
            <div>
              <div class="flash-item-name">{p.fullname ?? p.name}</div>
              <div class="flash-item-price">{p.deposit}</div>
            </div>
            {p.discount && <span class="flash-badge">{p.discount}</span>}
          </a>
        ))}
      </div>
    </div>
  )}

  <!-- TRUST BAR -->
  <div class="trust-bar" role="region" aria-label="Trust signals">
    <div class="trust-inner">
      <div class="trust-item">
        <div class="trust-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        Deposit from KSh 2,000
      </div>
      <div class="trust-item">
        <div class="trust-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg></div>
        No Credit Check
      </div>
      <div class="trust-item">
        <div class="trust-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12H3l9-9 9 9h-2"/><path d="M5 12v7a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-7"/></svg></div>
        Same-Day Delivery Nairobi
      </div>
      <div class="trust-item">
        <div class="trust-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg></div>
        Pay via M-Pesa on Delivery
      </div>
      <div class="trust-item">
        <div class="trust-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        100% Genuine Phones
      </div>
    </div>
  </div>

  <!-- PAGE BODY -->
  <main class="page-wrap">

    <!-- BRANDS -->
    <section aria-label="Shop by brand">
      <div class="section-head">
        <div class="section-head-left"><h2>Popular Brands</h2></div>
        <a href="/phones/lipa-mdogo-mdogo-nairobi" class="see-all">All brands <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></a>
      </div>
      <div class="brand-grid" role="list">
        <a href="/phones/tecno" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 116 34" width="96" height="28"><text x="0" y="28" font-family="Arial Black, sans-serif" font-size="28" font-weight="900" fill="#0057A8" letter-spacing="-0.5">TECNO</text></svg></div>
          <div class="brand-chip-sub">Tecno</div>
        </a>
        <a href="/phones/redmi" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 38" width="100" height="32"><rect x="0" y="2" width="32" height="32" rx="8" fill="#FF6900"/><text x="4" y="25" font-family="Arial, sans-serif" font-size="19" font-weight="900" fill="white">mi</text><text x="38" y="25" font-family="Arial, sans-serif" font-size="17" font-weight="700" fill="#FF6900">Redmi</text></svg></div>
          <div class="brand-chip-sub">Redmi</div>
        </a>
        <a href="/phones/samsung" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 160 30" width="120" height="24"><text x="0" y="24" font-family="Arial, sans-serif" font-size="22" font-weight="400" fill="#1428A0" letter-spacing="1">SAMSUNG</text></svg></div>
          <div class="brand-chip-sub">Samsung</div>
        </a>
        <a href="/phones/oppo" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 96 36" width="76" height="30"><text x="0" y="30" font-family="Arial, sans-serif" font-size="32" font-weight="300" fill="#1D8348" letter-spacing="2">oppo</text></svg></div>
          <div class="brand-chip-sub">Oppo</div>
        </a>
        <a href="/phones/infinix" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 46" width="100" height="38"><rect x="0" y="0" width="120" height="26" rx="4" fill="#00A651"/><text x="6" y="19" font-family="Arial Black, sans-serif" font-size="14" font-weight="900" fill="white" letter-spacing="0.3">Infinix</text><text x="2" y="42" font-family="Arial, sans-serif" font-size="10" fill="#00A651" font-style="italic">The Future is Now!</text></svg></div>
          <div class="brand-chip-sub">Infinix</div>
        </a>
        <a href="/phones/vivo" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 84 36" width="68" height="30"><text x="0" y="30" font-family="Arial, sans-serif" font-size="32" font-weight="300" fill="#415FFF" letter-spacing="1">vivo</text></svg></div>
          <div class="brand-chip-sub">Vivo</div>
        </a>
        <a href="/phones/itel" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 80 40" width="64" height="34"><text x="2" y="32" font-family="Georgia, 'Times New Roman', serif" font-size="30" font-weight="700" fill="#C0392B" font-style="italic">itel</text></svg></div>
          <div class="brand-chip-sub">Itel</div>
        </a>
        <a href="/phones/iphone" class="brand-chip" role="listitem">
          <div class="brand-logo-wrap"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 814 1000" width="28" height="34"><path fill="#1d1d1f" d="M788.1 340.9c-5.8 4.5-108.2 62.2-108.2 190.5 0 148.4 130.3 200.9 134.2 202.2-.6 3.2-20.7 71.9-68.7 141.9-42.8 61.6-87.5 123.1-155.5 123.1s-85.5-39.5-164-39.5c-76 0-103.7 40.8-165.9 40.8s-105-42.3-150.3-109.7C67.7 729.8 25.3 617.7 25.3 511.1c0-196.3 104.9-299.8 203.8-299.8 75.9 0 138.7 50 186.4 50 45.5 0 117.1-53.5 200.7-53.5 32.3 0 133.1 2.9 190.5 108.9zm-234-181.5c31.1-36.9 53.1-88.1 53.1-139.3 0-7.1-.6-14.3-1.9-20.1-50.6 1.9-110.8 33.7-147.1 75.8-28.5 32.4-55.1 83.6-55.1 135.5 0 7.8 1.3 15.6 1.9 18.1 3.2.6 8.4 1.3 13.6 1.3 45.4 0 102.5-30.4 135.5-71.3z"/></svg></div>
          <div class="brand-chip-sub">iPhones</div>
        </a>
      </div>
    </section>

    <hr class="section-rule" />

    <!-- TRENDING OFFERS -->
    <section aria-label="Trending offers">
      <div class="section-head">
        <div class="section-head-left">
          <h2>Trending Offers</h2>
          <span class="tag tag-red">Hot</span>
        </div>
        <a href="/phones/lipa-mdogo-mdogo-nairobi" class="see-all">View all <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></a>
      </div>
      <div class="product-grid" role="list">
        {featured.map(p => {
          const statusLower = p.status?.toLowerCase();
          const badgeClass  = statusLower === 'new' ? 'badge-new' : statusLower === 'hot' ? 'badge-hot' : 'badge-sale';
          const rating      = p.reviews?.averageRating ?? 4.5;
          const reviewCount = p.reviews?.reviewCount ?? 0;
          const specs       = [p.phonedisplay, p.processor?.split(' ').slice(-2).join(' '), p.battery?.split(',')[0]].filter(Boolean).slice(0, 3);
          return (
            <a href={`/${p.slug}`} class="card" role="listitem">
              {p.status && <span class={`card-badge ${badgeClass}`}>{p.status}</span>}
              {p.discount && <span class="card-discount">{p.discount}</span>}
              <div class="card-img-wrap">
                <img class="card-img" src={p.source} alt={`${p.fullname ?? p.name} — Lipa Mdogo Mdogo Nairobi`} loading="lazy" width="300" height="225" decoding="async" />
              </div>
              <div class="card-body">
                <div class="card-brand">{p.brand}</div>
                <h3 class="card-name">{p.fullname ?? p.name}</h3>
                <div class="card-specs">{specs.map(s => <span class="spec-pill">{s}</span>)}</div>
                <div class="card-rating"><span class="stars">{renderStars(rating)}</span><span class="rating-count">({reviewCount})</span></div>
                <div class="card-price">
                  <span class="price-deposit">{p.deposit}</span>
                  <span class="price-label">deposit</span>
                </div>
                <div class="installment-note">✓ Small installments &middot; Deposit {p.deposit}</div>
                <div class="card-cta">
                  <button class="btn-cart">Add to Cart</button>
                  <button class="btn-buy">Buy Now</button>
                </div>
              </div>
            </a>
          );
        })}
      </div>
    </section>

    <!-- BUDGET PICKS -->
    {budgetPicks.length > 0 && (
      <div class="budget-section" role="region" aria-label="Budget phones under KSh 5000 deposit">
        <div class="budget-head">
          <h2>Budget Picks — <span>Under KSh 5,000 Deposit</span></h2>
          <a href="/phones/deposit-under-5000" class="budget-see-all">See all budget phones →</a>
        </div>
        <div class="budget-grid">
          {budgetPicks.map(p => (
            <a href={`/${p.slug}`} class="budget-card">
              <div class="budget-img-wrap">
                <img class="budget-img" src={p.source} alt={`${p.fullname ?? p.name} cheap phone Nairobi installment`} loading="lazy" width="200" height="150" />
              </div>
              <div class="budget-body">
                <div class="budget-name">{p.fullname ?? p.name}</div>
                <div>
                  <div class="budget-deposit">{p.deposit}</div>
                  <div class="budget-deposit-lbl">deposit only</div>
                </div>
              </div>
              <span class="budget-cta">Get This Phone →</span>
            </a>
          ))}
        </div>
      </div>
    )}

    <!-- PROMO BANNERS -->
    <div class="promo-banners" role="region" aria-label="Promotional offers">
      <div class="promo-banner promo-banner-a">
        <div class="promo-text">
          <h3>Deposit Under KSh 3,000 — Take a Phone Home Today</h3>
          <p>Pay just KSh 2,000–2,999 deposit and walk out with your phone. Spread the rest weekly via M-Pesa. Zero paperwork.</p>
          <a href="/phones/deposit-under-3000" class="promo-btn">Shop Now →</a>
        </div>
        <div class="promo-badge">
          <div class="big">KSh 3K</div>
          <div class="small">Max Deposit</div>
        </div>
      </div>
      <div class="promo-banner promo-banner-b">
        <div class="promo-text">
          <h3>Free Delivery to Nairobi, Kiambu, Machakos &amp; Kajiado</h3>
          <p>Order before 2 PM. We deliver same day. Inspect the phone before you pay a single shilling.</p>
          <a href="/phones/lipa-mdogo-mdogo-nairobi" class="promo-btn">Order Now →</a>
        </div>
        <div class="promo-badge">
          <div class="big">FREE</div>
          <div class="small">Delivery</div>
        </div>
      </div>
    </div>

    <hr class="section-rule" />

    <!-- SAMSUNG -->
    <section aria-label="Samsung phones on installment Nairobi">
      <div class="section-head">
        <div class="section-head-left">
          <h2>Samsung Phones</h2>
          <span class="tag tag-blue">Lipa Mdogo Mdogo</span>
        </div>
        <a href="/phones/samsung" class="see-all">All Samsung <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></a>
      </div>
      <div class="product-grid" role="list">
        {samsungList.map(p => {
          const statusLower = p.status?.toLowerCase();
          const badgeClass  = statusLower === 'new' ? 'badge-new' : statusLower === 'hot' ? 'badge-hot' : 'badge-sale';
          const rating      = p.reviews?.averageRating ?? 4.5;
          const reviewCount = p.reviews?.reviewCount ?? 0;
          const specs       = [p.phonedisplay, p.processor?.split(' ').slice(-2).join(' '), p.battery?.split(',')[0]].filter(Boolean).slice(0, 3);
          return (
            <a href={`/${p.slug}`} class="card" role="listitem">
              {p.status && <span class={`card-badge ${badgeClass}`}>{p.status}</span>}
              {p.discount && <span class="card-discount">{p.discount}</span>}
              <div class="card-img-wrap">
                <img class="card-img" src={p.source} alt={`${p.fullname ?? p.name} Samsung lipa mdogo mdogo Nairobi`} loading="lazy" width="300" height="225" decoding="async" />
              </div>
              <div class="card-body">
                <div class="card-brand">{p.brand}</div>
                <h3 class="card-name">{p.fullname ?? p.name}</h3>
                <div class="card-specs">{specs.map(s => <span class="spec-pill">{s}</span>)}</div>
                <div class="card-rating"><span class="stars">{renderStars(rating)}</span><span class="rating-count">({reviewCount})</span></div>
                <div class="card-price"><span class="price-deposit">{p.deposit}</span><span class="price-label">deposit</span></div>
                <div class="installment-note">✓ From {p.from}/wk &middot; Deposit {p.deposit}</div>
                <div class="card-cta"><button class="btn-cart">Add to Cart</button><button class="btn-buy">Buy Now</button></div>
              </div>
            </a>
          );
        })}
      </div>
    </section>

    <hr class="section-rule" />

    <!-- TECNO -->
    <section aria-label="Tecno phones on installment Nairobi">
      <div class="section-head">
        <div class="section-head-left">
          <h2>Tecno Phones</h2>
          <span class="tag tag-ink">Best Value</span>
        </div>
        <a href="/phones/tecno" class="see-all">All Tecno <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></a>
      </div>
      <div class="product-grid" role="list">
        {tecnoList.map(p => {
          const statusLower = p.status?.toLowerCase();
          const badgeClass  = statusLower === 'new' ? 'badge-new' : statusLower === 'hot' ? 'badge-hot' : 'badge-sale';
          const rating      = p.reviews?.averageRating ?? 4.5;
          const reviewCount = p.reviews?.reviewCount ?? 0;
          const specs       = [p.phonedisplay, p.processor?.split(' ').slice(-2).join(' '), p.battery?.split(',')[0]].filter(Boolean).slice(0, 3);
          return (
            <a href={`/${p.slug}`} class="card" role="listitem">
              {p.status && <span class={`card-badge ${badgeClass}`}>{p.status}</span>}
              {p.discount && <span class="card-discount">{p.discount}</span>}
              <div class="card-img-wrap">
                <img class="card-img" src={p.source} alt={`${p.fullname ?? p.name} Tecno lipa mdogo mdogo Nairobi`} loading="lazy" width="300" height="225" decoding="async" />
              </div>
              <div class="card-body">
                <div class="card-brand">{p.brand}</div>
                <h3 class="card-name">{p.fullname ?? p.name}</h3>
                <div class="card-specs">{specs.map(s => <span class="spec-pill">{s}</span>)}</div>
                <div class="card-rating"><span class="stars">{renderStars(rating)}</span><span class="rating-count">({reviewCount})</span></div>
                <div class="card-price"><span class="price-deposit">{p.deposit}</span><span class="price-label">deposit</span></div>
                <div class="installment-note">✓ From {p.from}/wk &middot; Deposit {p.deposit}</div>
                <div class="card-cta"><button class="btn-cart">Add to Cart</button><button class="btn-buy">Buy Now</button></div>
              </div>
            </a>
          );
        })}
      </div>
    </section>

    <hr class="section-rule" />

    <!-- INFINIX -->
    <section aria-label="Infinix phones on installment Nairobi">
      <div class="section-head">
        <div class="section-head-left">
          <h2>Infinix Phones</h2>
          <span class="tag tag-ink">New Arrivals</span>
        </div>
        <a href="/phones/infinix" class="see-all">All Infinix <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></a>
      </div>
      <div class="product-grid" role="list">
        {infinixList.map(p => {
          const statusLower = p.status?.toLowerCase();
          const badgeClass  = statusLower === 'new' ? 'badge-new' : statusLower === 'hot' ? 'badge-hot' : 'badge-sale';
          const rating      = p.reviews?.averageRating ?? 4.5;
          const reviewCount = p.reviews?.reviewCount ?? 0;
          const specs       = [p.phonedisplay, p.processor?.split(' ').slice(-2).join(' '), p.battery?.split(',')[0]].filter(Boolean).slice(0, 3);
          return (
            <a href={`/${p.slug}`} class="card" role="listitem">
              {p.status && <span class={`card-badge ${badgeClass}`}>{p.status}</span>}
              {p.discount && <span class="card-discount">{p.discount}</span>}
              <div class="card-img-wrap">
                <img class="card-img" src={p.source} alt={`${p.fullname ?? p.name} Infinix installment Nairobi`} loading="lazy" width="300" height="225" decoding="async" />
              </div>
              <div class="card-body">
                <div class="card-brand">{p.brand}</div>
                <h3 class="card-name">{p.fullname ?? p.name}</h3>
                <div class="card-specs">{specs.map(s => <span class="spec-pill">{s}</span>)}</div>
                <div class="card-rating"><span class="stars">{renderStars(rating)}</span><span class="rating-count">({reviewCount})</span></div>
                <div class="card-price"><span class="price-deposit">{p.deposit}</span><span class="price-label">deposit</span></div>
                <div class="installment-note">✓ From {p.from}/wk &middot; Deposit {p.deposit}</div>
                <div class="card-cta"><button class="btn-cart">Add to Cart</button><button class="btn-buy">Buy Now</button></div>
              </div>
            </a>
          );
        })}
      </div>
    </section>

    <hr class="section-rule" />

    <!-- WHY CHOOSE US -->
    <section aria-label="Why choose MdogomdogoDeals">
      <div class="section-head">
        <div class="section-head-left"><h2>Why Choose MdogomdogoDeals?</h2></div>
      </div>
      <div class="why-grid">
        <div class="why-card">
          <div class="why-icon-wrap"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg></div>
          <div class="why-title">No Credit Check</div>
          <div class="why-desc">No CRB score needed. Pay your deposit and take your phone home today. Open to everyone in Nairobi.</div>
        </div>
        <div class="why-card">
          <div class="why-icon-wrap"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg></div>
          <div class="why-title">Pay via M-Pesa</div>
          <div class="why-desc">All deposits and installments via M-Pesa. Flexible daily, weekly or monthly payment plans.</div>
        </div>
        <div class="why-card">
          <div class="why-icon-wrap"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12H3l9-9 9 9h-2"/><path d="M5 12v7a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-7"/></svg></div>
          <div class="why-title">Same-Day Delivery</div>
          <div class="why-desc">Order before 2 PM and receive your phone in Nairobi, Kiambu, Machakos or Kajiado the same day.</div>
        </div>
        <div class="why-card">
          <div class="why-icon-wrap"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg></div>
          <div class="why-title">100% Genuine Phones</div>
          <div class="why-desc">Every phone comes with full manufacturer warranty. We sell zero clones or fakes — guaranteed.</div>
        </div>
      </div>
    </section>

  </main>

  <!-- HOW IT WORKS -->
  <section class="how-section" aria-label="How lipa mdogo mdogo works">
    <div class="how-inner">
      <div class="how-header">
        <h2>How Lipa Mdogo Mdogo Works</h2>
        <p>Get your phone in 4 simple steps — no paperwork, no guarantor required</p>
      </div>
      <div class="how-grid">
        <div class="how-card">
          <div class="how-num how-num-1">1</div>
          <div class="how-title">Order by Phone, WhatsApp or Online</div>
          <div class="how-desc">Browse {products.length}+ smartphones and place your order in under 2 minutes. No forms, no stress.</div>
        </div>
        <div class="how-card">
          <div class="how-num how-num-2">2</div>
          <div class="how-title">We Deliver to Your Door</div>
          <div class="how-desc">Free same-day delivery across Nairobi, Kiambu, Machakos and Kajiado counties.</div>
        </div>
        <div class="how-card">
          <div class="how-num how-num-3">3</div>
          <div class="how-title">Inspect, Then Pay Deposit</div>
          <div class="how-desc">Verify it's the exact phone you ordered. Pay the deposit via M-Pesa only when you're satisfied.</div>
        </div>
        <div class="how-card">
          <div class="how-num how-num-4">4</div>
          <div class="how-title">Phone is Yours — Pay in Installments</div>
          <div class="how-desc">Take your phone home. Pay the balance in daily, weekly or monthly M-Pesa installments that suit you.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- TESTIMONIALS -->
  <section class="testimonials-section" aria-label="Customer reviews">
    <div class="testimonials-inner">
      <div class="testimonials-head">
        <h2>What Our Customers Say</h2>
        <p>Thousands of Nairobi residents have already gotten their phones through us</p>
      </div>
      <div class="testimonials-grid">
        <div class="testi-card">
          <div class="testi-stars">★★★★★</div>
          <p class="testi-text">"Niliagiza Samsung A16 na waliwasilisha same day. Nilikagua kwanza kabla ya kulipa deposit. Process ilikuwa rahisi sana. Naipendekeza sana!"</p>
          <div class="testi-footer">
            <div class="testi-avatar">JM</div>
            <div><div class="testi-name">James M.</div><div class="testi-loc">Nairobi, Eastlands</div></div>
          </div>
        </div>
        <div class="testi-card">
          <div class="testi-stars">★★★★★</div>
          <p class="testi-text">"I paid KSh 2,500 deposit and took the Tecno Camon home. Weekly instalments via M-Pesa are very manageable. No CRB check — that's a game changer."</p>
          <div class="testi-footer">
            <div class="testi-avatar">AW</div>
            <div><div class="testi-name">Amina W.</div><div class="testi-loc">Kiambu, Ruiru</div></div>
          </div>
        </div>
        <div class="testi-card">
          <div class="testi-stars">★★★★★</div>
          <p class="testi-text">"Genuine phones, fast delivery, and excellent customer service. I've now bought three phones from MdogomdogoDeals for my family. Highly recommended!"</p>
          <div class="testi-footer">
            <div class="testi-avatar">PO</div>
            <div><div class="testi-name">Peter O.</div><div class="testi-loc">Nairobi, South B</div></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq-section" aria-label="Frequently asked questions">
    <div class="faq-inner">
      <div class="faq-head">
        <h2>Frequently Asked Questions</h2>
        <p>Everything you need to know about buying a phone on installments in Nairobi</p>
      </div>

      <div class="faq-item">
        <button class="faq-q" aria-expanded="false">What is the minimum deposit to get a phone on lipa mdogo mdogo?<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></button>
        <div class="faq-a">The minimum deposit is <strong>KSh 2,000</strong>. Browse our <a href="/phones/deposit-under-3000">phones under KSh 3,000 deposit</a> for the most affordable options. The deposit varies by model, and the remaining balance is paid via M-Pesa in daily, weekly or monthly installments.</div>
      </div>

      <div class="faq-item">
        <button class="faq-q" aria-expanded="false">Do I need a CRB check or guarantor to buy on installments?<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></button>
        <div class="faq-a">No. MdogomdogoDeals does <strong>not require a CRB check, guarantor, or any credit history</strong>. Pay the deposit via M-Pesa and the phone is yours — accessible to everyone in Nairobi and surrounding counties.</div>
      </div>

      <div class="faq-item">
        <button class="faq-q" aria-expanded="false">How does delivery work? Do you deliver outside Nairobi CBD?<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></button>
        <div class="faq-a">We offer <strong>free same-day delivery</strong> across Nairobi, Kiambu, Machakos and Kajiado counties for orders placed before 2 PM. You inspect the phone first — only pay the deposit after you're 100% satisfied.</div>
      </div>

      <div class="faq-item">
        <button class="faq-q" aria-expanded="false">Are the phones genuine? Do they come with a warranty?<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></button>
        <div class="faq-a">Yes. Every phone is <strong>100% original and brand new</strong>, sourced from authorized distributors, and comes with the full manufacturer warranty. We sell zero clones or refurbished units.</div>
      </div>

      <div class="faq-item">
        <button class="faq-q" aria-expanded="false">Which brands are available on lipa mdogo mdogo?<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></button>
        <div class="faq-a">We stock {products.length}+ smartphones including <a href="/phones/samsung">Samsung</a>, <a href="/phones/tecno">Tecno</a>, <a href="/phones/infinix">Infinix</a>, <a href="/phones/redmi">Redmi</a>, <a href="/phones/oppo">Oppo</a>, <a href="/phones/vivo">Vivo</a>, <a href="/phones/itel">Itel</a> and <a href="/phones/iphone">iPhones</a>. New models are added regularly.</div>
      </div>
    </div>
  </section>

  <!-- SEO CONTENT -->
  <section class="seo-content" aria-label="About lipa mdogo mdogo phones Nairobi">
    <div class="seo-content-inner">
      <h2>Buy Phones on Installments in Nairobi — Lipa Mdogo Mdogo via M-Pesa</h2>
      <p>
        MdogomdogoDeals is Nairobi's leading <strong>lipa mdogo mdogo phone shop</strong>, offering over {products.length} smartphones on flexible M-Pesa installment plans. Whether you're looking for a <a href="/phones/samsung">Samsung lipa mdogo mdogo</a>, <a href="/phones/tecno">Tecno installment phone</a>, or an <a href="/phones/infinix">Infinix on credit in Nairobi</a>, we have options starting from a deposit of just KSh 2,000.
      </p>
      <p>
        Unlike traditional hire purchase schemes, our <strong>pay weekly phone Nairobi</strong> model requires no paperwork, no CRB check, and no guarantor. Order online or via WhatsApp, receive free delivery to Nairobi, Kiambu, Machakos or Kajiado, inspect the phone, pay your deposit via M-Pesa and it's yours. Continue with <a href="/phones/deposit-under-5000">small daily or weekly installments</a> until the balance is cleared.
      </p>
      <p>
        We stock the latest models including <a href="/phones/deposit-under-3000">phones with deposits under KSh 3,000</a>, <a href="/phones/storage-128gb">128GB phones on installment</a>, and <a href="/phones/storage-256gb">256GB smartphones via M-Pesa</a>. All phones are 100% genuine with full manufacturer warranty. Experience the easiest way to <strong>buy a phone on credit in Kenya</strong> — without the stress.
      </p>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer" role="contentinfo">
    <div class="footer-inner">
      <div class="footer-grid">
        <div>
          <a href="/" class="footer-logo">Mdogo<em>mdogo</em>Deals</a>
          <p class="footer-tagline">Nairobi's trusted lipa mdogo mdogo phone shop. Genuine smartphones on flexible M-Pesa installment plans. No credit check. No stress. Free delivery.</p>
          <div class="footer-contact">
            <a href="https://wa.me/254100028823">📲 WhatsApp: 0100 028 823</a>
            <a href="tel:+254100028823">📞 Call: 0100 028 823</a>
            <a href="mailto:hello@mdogomdogodeals.co.ke">✉ hello@mdogomdogodeals.co.ke</a>
            <p style="font-size:12px;color:#334155;margin-top:8px;">Nairobi CBD — Mon–Sat 8AM–8PM</p>
          </div>
        </div>
        <div>
          <div class="footer-col-title">Top Brands</div>
          <ul class="footer-links">
            {ALL_BRANDS.map(brand => (
              <li><a href={`/phones/${brandSlug(brand)}`}>{brand} lipa mdogo mdogo</a></li>
            ))}
          </ul>
        </div>
        <div>
          <div class="footer-col-title">Shop by Deposit</div>
          <ul class="footer-links">
            {DEPOSIT_FILTERS.map(dep => (
              <li><a href={`/phones/${dep.key}`}>{dep.label} phone Nairobi</a></li>
            ))}
            <li><a href="/phones/lipa-mdogo-mdogo-nairobi">All phones on installment</a></li>
          </ul>
          <div class="footer-col-title" style="margin-top:16px;">Shop by Storage</div>
          <ul class="footer-links">
            {STORAGE_FILTERS.map(stor => (
              <li><a href={`/phones/${stor.key}`}>{stor.label} phone lipa mdogo mdogo</a></li>
            ))}
          </ul>
        </div>
        <div>
          <div class="footer-col-title">Quick Links</div>
          <ul class="footer-links">
            <li><a href="/">Home</a></li>
            <li><a href="/phones/lipa-mdogo-mdogo-nairobi">All Phones on Installment</a></li>
            <li><a href="/">How Lipa Mdogo Mdogo Works</a></li>
            <li><a href="/">About MdogomdogoDeals</a></li>
            <li><a href="/">Contact Us</a></li>
            <li><a href="/">Privacy Policy</a></li>
          </ul>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025 MdogomdogoDeals. All rights reserved.</p>
        <p>Lipa Mdogo Mdogo Phones Nairobi &middot; Pay via M-Pesa &middot; Free Delivery Nairobi</p>
      </div>
    </div>
  </footer>

  <!-- SEO KEYWORD TAGS -->
  <nav class="seo-tags" aria-label="Browse by keyword">
    <div class="seo-tags-inner">
      <a href="/phones/lipa-mdogo-mdogo-nairobi" class="seo-tag">lipa mdogo mdogo phones nairobi</a>
      <a href="/phones/lipa-mdogo-mdogo-nairobi" class="seo-tag">buy phone pay later kenya</a>
      <a href="/phones/lipa-mdogo-mdogo-nairobi" class="seo-tag">lipa pole pole phone nairobi</a>
      <a href="/phones/lipa-mdogo-mdogo-nairobi" class="seo-tag">pay weekly phone mpesa nairobi</a>
      <a href="/phones/lipa-mdogo-mdogo-nairobi" class="seo-tag">phone installments nairobi no credit check</a>
      {DEPOSIT_FILTERS.map(dep => (
        <a href={`/phones/${dep.key}`} class="seo-tag">{dep.label} phone nairobi</a>
      ))}
      {ALL_BRANDS.map(brand => (
        <a href={`/phones/${brandSlug(brand)}`} class="seo-tag">{brand} lipa mdogo mdogo nairobi</a>
      ))}
      <a href="/phones/storage-128gb" class="seo-tag">128gb phone lipa mdogo mdogo</a>
      <a href="/phones/storage-256gb" class="seo-tag">256gb phone installment nairobi</a>
      <a href="/phones/deposit-under-3000" class="seo-tag">phone deposit under 3000 nairobi</a>
    </div>
  </nav>

  <script>
    document.querySelectorAll('.faq-q').forEach(btn => {
      btn.addEventListener('click', () => {
        const isOpen = btn.classList.contains('open');
        document.querySelectorAll('.faq-q').forEach(b => {
          b.classList.remove('open');
          b.setAttribute('aria-expanded', 'false');
          b.nextElementSibling?.classList.remove('open');
        });
        if (!isOpen) {
          btn.classList.add('open');
          btn.setAttribute('aria-expanded', 'true');
          btn.nextElementSibling?.classList.add('open');
        }
      });
    });
  </script>

</body>
</html>
