<style>
  :root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --accent-red: #ff4757;
    --text-dark: #2d3436;
    --text-gray: #636e72;
    --white: #ffffff;
    --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  }

  /* Layout Container */
  .promo-section {
    max-width: 1200px;
    margin: 40px auto;
    padding: 0 20px;
    font-family: "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  }

  .promo-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 20px;
  }

  /* Main Promo Card */
  .card-main {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    background: #f9f9f9;
    border-radius: 15px;
    padding: 40px;
    align-items: center;
    overflow: hidden;
  }

  /* Side Promo Card */
  .card-side {
    background: #eee url("/phones/p1.png") no-content center/contain no-repeat;
    border-radius: 15px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 30px;
    text-align: center;
    transition: var(--transition);
  }

  .card-side:hover {
    transform: translateY(-5px);
  }

  /* Typography */
  .badge {
    display: inline-block;
    background: var(--accent-red);
    color: white;
    padding: 5px 12px;
    border-radius: 50px;
    font-weight: bold;
    font-size: 14px;
    margin-bottom: 15px;
  }

  .promo-title {
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    line-height: 1.2;
    margin: 10px 0;
    color: var(--text-dark);
    font-weight: 800;
  }

  .promo-subtitle {
    font-size: 1rem;
    color: var(--text-gray);
    letter-spacing: 1px;
    margin-bottom: 25px;
    text-transform: uppercase;
  }

  /* Product Image */
  .product-img-wrapper {
    position: relative;
    transition: var(--transition);
  }

  .product-img-wrapper img {
    width: 100%;
    height: auto;
    object-fit: contain;
    filter: drop-shadow(0 10px 15px rgba(0, 0, 0, 0.1));
  }

  .product-img-wrapper:hover {
    transform: scale(1.05);
  }

  /* Interactive Button */
  .btn-shop {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 12px 28px;
    background: var(--primary-gradient);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
    transition: var(--transition);
  }

  .btn-shop:hover {
    box-shadow: 0 15px 30px rgba(102, 126, 234, 0.4);
    transform: translateY(-2px);
  }

  .btn-shop svg {
    width: 18px;
    transition: var(--transition);
  }
  .btn-shop:hover svg {
    transform: translateX(5px);
  }

  /* Small Card Style */
  .brand-label {
    background: rgba(255, 255, 255, 0.9);
    padding: 10px;
    border-radius: 8px;
    font-weight: bold;
    color: var(--text-dark);
    text-decoration: none;
  }

  /* Responsive Design */
  @media (max-width: 992px) {
    .promo-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 600px) {
    .card-main {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 30px 20px;
    }
    .product-img-wrapper {
      order: -1;
      margin-bottom: 20px;
    }
  }
</style>

<section class="promo-section">
  <div class="promo-grid">
    <article class="card-main">
      <div class="content">
        <span class="badge">HOT SALE %</span>
        <h1 class="promo-title">Latest Tecno on<br />Lipa Mdogo Mdogo</h1>
        <p class="promo-subtitle">Amazing Discounts in Nairobi</p>

        <a href="#" class="btn-shop">
          SHOP NOW
          <svg
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </a>
      </div>

      <div class="product-img-wrapper">
        <a href="/tecno">
          <img src="/phones/tecnocamon40pro.webp" alt="Tecno Camon 40 Pro" />
        </a>
      </div>
    </article>

    <a href="/samsung" class="card-side">
      <span class="brand-label">Shop Samsung</span>
    </a>
  </div>
</section>
