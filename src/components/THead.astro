<section class="lmm-hero-wrapper">
  <div class="lmm-hero-inner">
    <div class="lmm-hero-card">

      <!-- LEFT COLUMN -->
      <div class="lmm-col-left">
        <div class="lmm-trust-badge">
          <span class="lmm-badge-dot"></span>
          <span>🇰🇪 Kenya's #1 Phone Installment Store &mdash; Trusted by 50,000+ Kenyans</span>
        </div>

        <h1 class="lmm-headline">
          Lipa Mdogo Mdogo<br />
          <em>Phones</em><br />
          &mdash; in Kenya
        </h1>

        <p class="lmm-sub">
          Get the latest <a href="/samsung" class="lmm-brand-link">Samsung</a>,
          <a href="/iphone" class="lmm-brand-link">iPhone</a>,
          <a href="/tecno" class="lmm-brand-link">Tecno</a>, and
          <a href="/infinix" class="lmm-brand-link">Infinix</a> phones with a small deposit.
          Pay the rest in small daily amounts via <strong>M-Pesa</strong> &mdash; no bank, no stress.
        </p>

        <ul class="lmm-features" role="list">
          <li>
            <span class="lmm-feat-icon" aria-hidden="true">💸</span>
            <div>
              <strong>Low Deposit</strong>
              <span>Start from as low as KSh 500 deposit</span>
            </div>
          </li>
          <li>
            <span class="lmm-feat-icon" aria-hidden="true">✅</span>
            <div>
              <strong>100% Genuine Phones</strong>
              <span>All devices are brand new &amp; verified</span>
            </div>
          </li>
          <li>
            <span class="lmm-feat-icon" aria-hidden="true">📅</span>
            <div>
              <strong>Flexible Daily Payments</strong>
              <span>Pay at your own pace via M-Pesa</span>
            </div>
          </li>
          <li>
            <span class="lmm-feat-icon" aria-hidden="true">🚚</span>
            <div>
              <strong>Free Delivery Nationwide</strong>
              <span>We deliver across all 47 counties</span>
            </div>
          </li>
        </ul>

        <div class="lmm-cta-group">
          <a href="/phones" class="lmm-btn lmm-btn-primary">
            <span>📱</span> Shop Phones
          </a>
          <a
            href="https://wa.me/254700000000?text=Habari%2C%20I%20want%20to%20order%20a%20phone%20on%20installment"
            class="lmm-btn lmm-btn-whatsapp"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span>💬</span> WhatsApp Order
          </a>
        </div>

        <div class="lmm-trust-row" role="list">
          <div class="lmm-trust-item" role="listitem">
            <strong>50K+</strong>
            <span>Happy Customers</span>
          </div>
          <div class="lmm-trust-divider" aria-hidden="true"></div>
          <div class="lmm-trust-item" role="listitem">
            <strong>200+</strong>
            <span>Phone Models</span>
          </div>
          <div class="lmm-trust-divider" aria-hidden="true"></div>
          <div class="lmm-trust-item" role="listitem">
            <strong>4.9 &#9733;</strong>
            <span>Customer Rating</span>
          </div>
          <div class="lmm-trust-divider" aria-hidden="true"></div>
          <div class="lmm-trust-item" role="listitem">
            <strong>Same Day</strong>
            <span>Nairobi Delivery</span>
          </div>
        </div>

      </div>

      <!-- RIGHT COLUMN -->
      <div class="lmm-col-right">
        <div class="lmm-how-card">
          <div class="lmm-how-header">
            <span class="lmm-how-pill">How It Works</span>
            <p class="lmm-how-subhead">Get your phone in 4 simple steps</p>
          </div>

          <ol class="lmm-steps" role="list">
            <li class="lmm-step">
              <div class="lmm-step-num" aria-label="Step 1">1</div>
              <div class="lmm-step-body">
                <strong>Choose Your Phone</strong>
                <p>Browse 200+ smartphones and pick your favourite model</p>
              </div>
            </li>
            <li class="lmm-step">
              <div class="lmm-step-num" aria-label="Step 2">2</div>
              <div class="lmm-step-body">
                <strong>Free Delivery to You</strong>
                <p>We deliver straight to your door &mdash; anywhere in Kenya</p>
              </div>
            </li>
            <li class="lmm-step">
              <div class="lmm-step-num" aria-label="Step 3">3</div>
              <div class="lmm-step-body">
                <strong>Pay Small Deposit</strong>
                <p>Secure your phone with an affordable first payment via M-Pesa</p>
              </div>
            </li>
            <li class="lmm-step lmm-step-highlight">
              <div class="lmm-step-num lmm-step-num-highlight" aria-label="Step 4">4</div>
              <div class="lmm-step-body">
                <strong>Lipa Mdogo Mdogo Daily 🎉</strong>
                <p>Pay tiny daily amounts from your M-Pesa &mdash; no pressure, no deadlines</p>
              </div>
            </li>
          </ol>

          <div class="lmm-how-footer">
            <a href="/how-it-works" class="lmm-learn-more">
              Learn more about our installment plan &rarr;
            </a>
          </div>
        </div>

        <!-- Floating badge -->
        <div class="lmm-float-badge" aria-hidden="true">
          <span class="lmm-float-icon">🔒</span>
          <div>
            <strong>M-Pesa Secured</strong>
            <span>Payments protected</span>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  /* ── Reset & Tokens ───────────────────────────────────────────── */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --lmm-orange: #f68b1e;
    --lmm-orange-dark: #e07b10;
    --lmm-orange-light: #fff3e0;
    --lmm-orange-xlight: #fff8f0;
    --lmm-orange-deep: #c96a00;
    --lmm-wa: #25D366;
    --lmm-wa-dark: #128C7E;
    --lmm-text-primary: #1a1a1a;
    --lmm-text-secondary: #555;
    --lmm-text-muted: #999;
    --lmm-border: rgba(0, 0, 0, 0.08);
    --lmm-radius-card: 20px;
    --lmm-radius-sm: 10px;
    --lmm-shadow-card: 0 4px 6px -1px rgba(0,0,0,.07), 0 20px 60px -10px rgba(0,0,0,.15);
    --lmm-shadow-sm: 0 2px 8px rgba(0,0,0,.08);
    --lmm-font: 'DM Sans', ui-sans-serif, system-ui, sans-serif;
    --lmm-gradient: linear-gradient(135deg, #c96a00 0%, #e07b10 40%, #f68b1e 100%);
  }

  /* ── Full-bleed wrapper ───────────────────────────────────────── */
  .lmm-hero-wrapper {
    position: relative;
    width: 100vw;
    margin-left: calc(50% - 50vw);
    margin-right: calc(50% - 50vw);
    background: var(--lmm-gradient);
    padding: 72px 24px 80px;
    overflow: hidden;
    font-family: var(--lmm-font);
  }

  /* Decorative orbs */
  .lmm-hero-wrapper::before,
  .lmm-hero-wrapper::after {
    content: '';
    position: absolute;
    border-radius: 50%;
    pointer-events: none;
  }
  .lmm-hero-wrapper::before {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255,255,255,0.07) 0%, transparent 70%);
    top: -150px;
    right: -100px;
  }
  .lmm-hero-wrapper::after {
    width: 350px;
    height: 350px;
    background: radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 70%);
    bottom: -100px;
    left: 5%;
  }

  /* ── Center container ─────────────────────────────────────────── */
  .lmm-hero-inner {
    max-width: 1160px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  /* ── White card ───────────────────────────────────────────────── */
  .lmm-hero-card {
    background: #ffffff;
    border-radius: var(--lmm-radius-card);
    border: 1px solid var(--lmm-border);
    box-shadow: var(--lmm-shadow-card);
    display: grid;
    grid-template-columns: 1fr 420px;
    gap: 0;
    overflow: hidden;
  }

  /* ── Left column ──────────────────────────────────────────────── */
  .lmm-col-left {
    padding: 52px 48px 52px 52px;
    display: flex;
    flex-direction: column;
    gap: 28px;
    border-right: 1px solid var(--lmm-border);
  }

  /* Trust badge */
  .lmm-trust-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--lmm-orange-xlight);
    border: 1px solid #fdd9a0;
    color: var(--lmm-orange-deep);
    border-radius: 999px;
    padding: 6px 14px;
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    width: fit-content;
  }

  .lmm-badge-dot {
    display: inline-block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--lmm-orange);
    animation: lmm-pulse 2s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes lmm-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.25); }
  }

  /* Headline */
  .lmm-headline {
    font-size: clamp(1.9rem, 3.2vw, 2.75rem);
    font-weight: 800;
    line-height: 1.18;
    letter-spacing: -0.03em;
    color: var(--lmm-text-primary);
  }

  .lmm-headline em {
    font-style: normal;
    background: linear-gradient(90deg, var(--lmm-orange-deep) 0%, var(--lmm-orange) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Supporting text */
  .lmm-sub {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--lmm-text-secondary);
    max-width: 480px;
  }

  .lmm-brand-link {
    color: var(--lmm-orange-dark);
    font-weight: 600;
    text-decoration: none;
    border-bottom: 1.5px solid #fcd09a;
    transition: border-color 0.2s, color 0.2s;
  }

  .lmm-brand-link:hover {
    color: var(--lmm-orange);
    border-color: var(--lmm-orange);
  }

  /* Feature list */
  .lmm-features {
    list-style: none;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
  }

  .lmm-features li {
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }

  .lmm-feat-icon {
    font-size: 1.4rem;
    line-height: 1;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .lmm-features li div {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .lmm-features li strong {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--lmm-text-primary);
  }

  .lmm-features li span {
    font-size: 0.78rem;
    color: var(--lmm-text-muted);
    line-height: 1.4;
  }

  /* CTAs */
  .lmm-cta-group {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .lmm-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 24px;
    border-radius: var(--lmm-radius-sm);
    font-family: var(--lmm-font);
    font-size: 0.95rem;
    font-weight: 700;
    text-decoration: none;
    cursor: pointer;
    transition: transform 0.15s, box-shadow 0.15s, background 0.2s;
    white-space: nowrap;
  }

  .lmm-btn:active {
    transform: scale(0.97);
  }

  .lmm-btn-primary {
    background: var(--lmm-gradient);
    color: #fff;
    box-shadow: 0 4px 14px rgba(246, 139, 30, 0.4);
  }

  .lmm-btn-primary:hover {
    box-shadow: 0 6px 20px rgba(246, 139, 30, 0.55);
    transform: translateY(-1px);
  }

  .lmm-btn-whatsapp {
    background: var(--lmm-wa);
    color: #fff;
    box-shadow: 0 4px 14px rgba(37, 211, 102, 0.3);
  }

  .lmm-btn-whatsapp:hover {
    background: var(--lmm-wa-dark);
    box-shadow: 0 6px 20px rgba(37, 211, 102, 0.45);
    transform: translateY(-1px);
  }

  /* Trust row */
  .lmm-trust-row {
    display: flex;
    align-items: center;
    gap: 20px;
    padding-top: 4px;
    flex-wrap: wrap;
  }

  .lmm-trust-item {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .lmm-trust-item strong {
    font-size: 1.05rem;
    font-weight: 800;
    color: var(--lmm-text-primary);
    line-height: 1;
  }

  .lmm-trust-item span {
    font-size: 0.72rem;
    color: var(--lmm-text-muted);
    white-space: nowrap;
  }

  .lmm-trust-divider {
    width: 1px;
    height: 28px;
    background: var(--lmm-border);
    flex-shrink: 0;
  }

  /* ── Right column ─────────────────────────────────────────────── */
  .lmm-col-right {
    padding: 40px 36px;
    background: #fafafa;
    display: flex;
    flex-direction: column;
    gap: 20px;
    position: relative;
  }

  /* How-it-works card */
  .lmm-how-card {
    background: #fff;
    border: 1px solid var(--lmm-border);
    border-radius: 16px;
    box-shadow: var(--lmm-shadow-sm);
    overflow: hidden;
  }

  .lmm-how-header {
    padding: 20px 24px 16px;
    background: var(--lmm-gradient);
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .lmm-how-pill {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.8);
  }

  .lmm-how-subhead {
    font-size: 1.05rem;
    font-weight: 700;
    color: #fff;
  }

  .lmm-steps {
    list-style: none;
    padding: 20px 24px;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .lmm-step {
    display: flex;
    align-items: flex-start;
    gap: 14px;
  }

  .lmm-step-num {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: var(--lmm-orange-light);
    color: var(--lmm-orange-deep);
    font-size: 0.85rem;
    font-weight: 800;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    border: 2px solid #fcd09a;
  }

  .lmm-step-body {
    display: flex;
    flex-direction: column;
    gap: 3px;
    padding-top: 4px;
  }

  .lmm-step-body strong {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--lmm-text-primary);
  }

  .lmm-step-body p {
    font-size: 0.78rem;
    color: var(--lmm-text-secondary);
    line-height: 1.5;
  }

  /* Highlighted last step */
  .lmm-step-highlight {
    background: var(--lmm-orange-xlight);
    border: 1.5px solid #fdd9a0;
    border-radius: 12px;
    padding: 14px;
    margin: 0 -4px;
  }

  .lmm-step-num-highlight {
    background: var(--lmm-orange);
    color: #fff;
    border-color: var(--lmm-orange-dark);
  }

  .lmm-how-footer {
    padding: 0 24px 20px;
  }

  .lmm-learn-more {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--lmm-orange-deep);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    transition: gap 0.15s;
  }

  .lmm-learn-more:hover {
    gap: 8px;
    text-decoration: underline;
  }

  /* Floating M-Pesa badge */
  .lmm-float-badge {
    display: flex;
    align-items: center;
    gap: 12px;
    background: #fff;
    border: 1px solid var(--lmm-border);
    border-radius: 12px;
    padding: 12px 16px;
    box-shadow: var(--lmm-shadow-sm);
  }

  .lmm-float-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .lmm-float-badge div {
    display: flex;
    flex-direction: column;
    gap: 1px;
  }

  .lmm-float-badge strong {
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--lmm-text-primary);
  }

  .lmm-float-badge span {
    font-size: 0.72rem;
    color: var(--lmm-text-muted);
  }

  /* ── Tablet (≤ 900px) ─────────────────────────────────────────── */
  @media (max-width: 900px) {
    .lmm-hero-wrapper {
      padding: 48px 20px 56px;
    }

    .lmm-hero-card {
      grid-template-columns: 1fr;
    }

    .lmm-col-left {
      padding: 40px 36px;
      border-right: none;
      border-bottom: 1px solid var(--lmm-border);
    }

    .lmm-col-right {
      padding: 36px 36px 40px;
    }

    .lmm-features {
      grid-template-columns: 1fr 1fr;
    }
  }

  /* ── Mobile (≤ 600px) ─────────────────────────────────────────── */
  @media (max-width: 600px) {
    .lmm-hero-wrapper {
      padding: 32px 16px 40px;
    }

    .lmm-hero-card {
      border-radius: 16px;
    }

    .lmm-col-left {
      padding: 32px 24px;
      gap: 22px;
    }

    .lmm-headline {
      font-size: clamp(1.6rem, 7vw, 2rem);
    }

    .lmm-features {
      grid-template-columns: 1fr;
    }

    .lmm-cta-group {
      flex-direction: column;
    }

    .lmm-btn {
      width: 100%;
      justify-content: center;
    }

    .lmm-trust-row {
      gap: 12px;
    }

    .lmm-trust-divider {
      height: 20px;
    }

    .lmm-col-right {
      padding: 24px 20px 28px;
    }

    .lmm-trust-badge {
      font-size: 0.72rem;
    }
  }
</style>